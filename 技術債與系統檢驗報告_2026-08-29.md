# Market Pulse 專案技術債與系統檢驗報告

檢驗日期：2026-08-29（Asia/Taipei）  
檢驗範圍：後端架構、前端頁面與資產、API 路由與連線、前後端整合、安全護欄、既有技術債清單  
結論：**核心程式與安全護欄大致穩定，但技術債尚未全部解決；即時資料 API 與完整前端回歸目前無法判定為全綠。**

## 一、執行摘要

- 後端已完成模組化：`app.py` 約 650 行，路由拆至 4 個 Blueprint；目前掃描出 49 條路由，其中 40 條 API 路由。
- 前端已切成 15 個 classic-script JavaScript 檔案、21 個 HTML 頁面；但仍保留 `app.js`，且前端大型函式/共享全域作用域等債項仍存在。
- 115 個單元測試通過，Python/JavaScript 語法檢查通過。
- 安全護欄 16 項全部通過，未發現本次檢驗造成的 SQL 參數化、XSS、CSP、安全標頭或 secrets 問題。
- E2E smoke 通過，但日誌顯示 TAIFEX/Yahoo 外部連線遭本機環境拒絕；因此不能視為即時資料源健康。
- 完整基準驗證退出碼 1：即時 API 有 500/502，前端 21 頁的 compare/interaction 被 `ERR_NETWORK_ACCESS_DENIED` 與外部資料缺失影響。
- `portable_check.py` 失敗：`/twse-data.js` 與 Stock search API 回傳 HTTP 500，這是目前最明確需要追查的產品/API 異常。

## 二、驗證結果

| 檢查項目 | 結果 | 證據/備註 |
|---|---|---|
| Python 語法 | 通過 | `py_compile` 通過全部主要後端模組 |
| JavaScript 語法 | 通過 | root JS 檔案 `node --check` 全部通過 |
| 單元測試 | 通過 | 115 tests，`OK` |
| 安全護欄 | 通過 | `SECURITY_GUARDRAIL_OK: 16 passed` |
| E2E smoke | 通過 | `E2E_SMOKE_OK`；同時有外部連線拒絕警告 |
| 路由掃描 | 通過 | 49 routes / 40 API routes |
| Release integrity | 通過 | DB unchanged、mock free、115 tests OK |
| Portable check | 失敗 | `/twse-data.js`、Stock search API HTTP 500 |
| 完整 API 基準 | 失敗/受外部服務影響 | `/api/twse/site-data` 500；live-sectors/overview/stocks 502 |
| 21 頁前端 compare | 失敗/受環境影響 | 40 項失敗，多為 `ERR_NETWORK_ACCESS_DENIED` 與畫面差異 |
| 94 條互動 compare | 失敗/受環境影響 | 20 項失敗，主要為相同網路拒絕 |

## 三、技術債是否已解決

### 已確認完成或已有護欄的部分

依 `docs/TD稽核清單.md` 與目前程式結構，以下項目已有修復紀錄或驗證支持：

- TD-01：後端由巨石 `app.py` 拆成 routes、fetchers、builders、parsers、cache、security 等模組。
- TD-02：前端已拆出 15 個 classic-script 切片，且 global symbol、load order、`escapeHtml` 單一來源均有護欄。
- TD-04：Git repository 已存在，且執行期資料檔有排除規範。
- TD-05：已有 `fetch_registry.py` 宣告式資料來源 registry。
- TD-18/21：路由掃描已支援 Blueprint，實測正確掃出 49 條路由。
- TD-20：release integrity 支援既有資料庫與 clean-package 模式，實測資料庫內容未被測試改動。
- TD-22：可攜包缺少 regression 目錄時可回報 SKIP，不再誤判為失敗。

### 尚未全部解決、仍應列為開放債項

目前不能宣稱「技術債已清零」。清單中仍明確標為待評估或保留風險的項目包括：

- TD-03：rate limit、快取、in-flight locks 仍是模組層記憶體狀態；部署仍依賴單 worker 假設。
- TD-07：SSL fallback 仍存在，現況是有 production gating，但尚未完成更嚴格的 opt-in/憑證鏈方案。
- TD-08：21 頁仍有多種 nav 變體，共用導覽尚未完全單一來源化。
- TD-09/12/14：前端資源、CSS、快取 TTL 與 eviction 仍有大型檔案/分散設定債務。
- TD-10：測試覆蓋率相對於整體後端規模仍不足，尤其大型 builder/fetcher 的特性測試。
- TD-11：`app.js` 同名 `worker` 函式問題仍需確認是否已完全處理。
- TD-13：broad exception 與行尾/可讀性問題仍有維護成本。
- TD-15：前端死碼與失效控制項仍需逐項清理或修復。
- TD-16：法人歷史資料筆數不足時的遞迴當機風險，需以本次前端互動測試在可用網路環境再次確認。
- TD-17/19：完整基準仍依賴即時外部資料，且 structure-only 比對對關鍵欄位的保護仍有限。
- TD-23/24：`app.py` import 副作用，以及測試造成 `__pycache__`/快取檔變動的問題仍列為待處理。

## 四、網頁架構與功能檢查

架構層面沒有發現語法或載入順序的立即性錯誤：21 個 HTML 頁面存在，JS 語法全部通過，安全護欄也確認前端未加入 inline script、`escapeHtml` 未重複定義。

但完整瀏覽器檢查不能判定為全正常，原因如下：

1. 多數頁面在載入 API 或外部資源時出現 `Failed to load resource: net::ERR_NETWORK_ACCESS_DENIED`。
2. 截圖差異比例約 6%～37%，與即時資料未載入、表格內容為空或 fallback 畫面有關，不能直接判定為 CSS/HTML 回歸。
3. `tw-stocks.html` 被記錄為 table rows 為 0；在外部資料被拒絕的條件下，這是功能可用性風險，但尚不足以確認前端渲染邏輯本身錯誤。
4. TD-15 的失效控制項與 TD-16 的低筆數遞迴路徑，仍應在允許 API/外部資料的環境中做定向互動驗證。

## 五、API 與前後端連接異常

### 已確認的 API 異常

- `/twse-data.js`：`portable_check.py` 收到 HTTP 500。
- Stock search API：`portable_check.py` 收到 HTTP 500。
- `/api/twse/site-data`：完整基準檢查的 fixture/即時檢查出現 HTTP 500。
- `/api/twse/live-sectors`：502，`LIVE_SECTORS_UNAVAILABLE`。
- `/api/twse/live-overview`：502，`LIVE_OVERVIEW_UNAVAILABLE`。
- `/api/twse/live-stocks`：502，`LIVE_STOCKS_UNAVAILABLE`。

### 外部環境造成、尚不能歸責產品的異常

E2E 與瀏覽器檢查記錄 TAIFEX、Yahoo、TWSE 相關連線出現 Windows `WinError 10013` 或瀏覽器 `ERR_NETWORK_ACCESS_DENIED`。這表示本次環境的網路政策/防火牆阻擋了外部資料源；由於服務本身有 fallback/error response，E2E smoke 仍能完成，但即時資料功能沒有被有效驗證。

## 六、風險分級與建議順序

### P0/P1：先確認可用性

1. 在允許對外 HTTPS 的環境重跑 `portable_check.py`、`verify_against_baseline.py --full`。
2. 若仍有 500，優先追 `/twse-data.js`、Stock search、`/api/twse/site-data` 的 server traceback 與 fallback 分支。
3. 確認 TWSE/TAIFEX/Yahoo 各資料源的 timeout、錯誤碼與空資料回應是否符合前端預期。

### P1：功能正確性

1. 針對法人歷史少於 5 筆的個股，確認 TD-16 是否仍可能遞迴或造成畫面當機。
2. 針對 options underlying 控制項，確認 TD-15 是刻意停用還是實際失效。
3. 為 Stock search、TWSE live overview、live stocks 增加固定 fixture/離線 API 測試，避免外部網路阻擋時無法區分產品錯誤。

### P2：架構與維運

處理 TD-03、TD-07、TD-08、TD-09、TD-10、TD-12、TD-14、TD-17、TD-19、TD-23、TD-24；這些不一定立即造成當機，但會持續增加部署、效能、測試可信度與維護風險。

## 七、最終判定

**目前判定：部分完成，尚未達到「全項技術債解決、網頁/API/前後端全綠」的標準。**

可以確認的正面結論是：模組化後端、基本前端拆分、安全護欄、語法、單元測試、E2E smoke 與 release integrity 均有良好結果。不能忽略的負面結論是：即時 TWSE 相關 API 存在 500/502、可攜性檢查失敗，且完整瀏覽器回歸受網路阻擋而失敗；另有多項技術債在稽核清單中仍屬待評估或未完成。

本報告沒有修改產品程式碼、基準檔或驗證工具。
