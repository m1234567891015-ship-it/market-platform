# Market Pulse 技術債改善計畫 V2

版本：V2  
規劃日期：2026-08-30  
前版：[技術債改善計畫_2026-08-29.md](./技術債改善計畫_2026-08-29.md)  
稽核來源：[docs/TD稽核清單.md](./docs/TD稽核清單.md)  
重構證據：[docs/REFACTOR_SUMMARY.md](./docs/REFACTOR_SUMMARY.md)

## 1. 計畫定位

本版不是重新開工，而是把 2026-07-16 的稽核快照與 A～G 的實際完成結果合併成目前可執行的工作清單：

- 已完成項目標記為已結案，不重複施工。
- 部分完成項目拆出剩餘工作，避免把短期護欄誤稱為長期解決。
- 可選的前端複雜度改善排入獨立小批次。
- TD-18、TD-19 與共享狀態切換保留為評估/設計工單，不直接混入低風險整理。

## 2. 目前基線與覆核結論

2026-08-30 覆核採用的現況證據：

- Unit：126 tests PASS。
- Security guardrail：16 項 PASS。
- production `app.js`/`js/*.js` 無 `worker` 同名函式；目前 `worker` 僅出現在 regression helper。
- 最近一次完整驗證：`e2e_smoke.py` 為 `E2E_SMOKE_OK`；`regression/verify_against_baseline.py --full` 為 `VERIFY_OK`，包含 live/cached API、21 頁 frontend、94 條 interaction 與 security。
- API、baseline、interaction、SQLite/cache side-effect 護欄均以既有批次紀錄為準；外部資料源不可用時仍須依 `[外部問題]` 分類，不更新 baseline 掩蓋問題。

### H-01～H-06 Baseline 來源追溯（2026-08-31）

- 已證實：`regression/baseline/frontend/global_symbols.json` 與 `regression/baseline/escapehtml_baseline.json` 由 H-04-11～H-04-15 的 TD-15 死碼移除批次同步更新；對應 git commits 為 `1214071`、`0e62c49`、`fd7e521`。
- 待補證據：`regression/baseline/frontend_manifest.json` 的 `derivatives-assets.html cards 31→32` 與 3 張 derivatives 截圖 binary diff，目前沒有 H-01～H-06 的批次／commit 紀錄可直接歸屬。H-01、H-02、H-03、H-05、H-06 的既有紀錄反而明載未更新相應 baseline。
- 詳細檔案清單與處理規則見 `docs/baseline_provenance_H01-H06_2026-08-31.md`；未補證據前不重新 capture、不覆寫、不將這 4 個檔案納入新的 baseline commit。

### TD 現況矩陣

| 狀態 | TD | 覆核結論 | 後續處理 |
|---|---|---|---|
| ✅ | TD-01 | 後端已拆為 routes/fetchers/builders/cache/security 等模組，`app.py` 為薄入口。 | 僅維護，不重新合併。 |
| 🟡 | TD-02 | 已完成 classic-script JS 切片與載入順序護欄；尚未導入 ES Modules、bundling、tree-shaking。 | 前端大型 render function 小批次拆分；打包另列 TD-18。 |
| 🟡 | TD-03 | 單 worker 契約、process-local 警告、上限與測試已完成；跨 worker 共享狀態仍未外移。 | 先做容量/一致性設計，未核准前不切 Redis/SQLite。 |
| ✅ | TD-04 | git、`.gitignore`、執行期資料分離與 portable proof 已建立。 | 維持交付包檢查。 |
| ✅ | TD-05 | 86 個 fetcher 已納入 registry/共用 HTTP 路徑並完成檢視。 | 新資料源依 registry 規範加入。 |
| ✅ | TD-06 | rate-limit identity/window/eviction/response 已拆分。 | 僅維護。 |
| ✅ | TD-07 | SSL fallback 為明確 opt-in、allowlist、verified-first、production block 並有 audit log。 | 僅維護與測試。 |
| ✅ | TD-08 | 21 頁導覽由單一來源產生，active state 已驗證。 | 僅維護。 |
| ✅ | TD-09 | service worker 已恢復 stale-while-revalidate，資源重用率已量測。 | 破壞性資產變更才遞增 cache version。 |
| ✅ | TD-10 | 已完成 10 個大型 builder/fetcher 的 fixture contract coverage matrix，並記錄成功、空資料/上游失敗案例與固定輸出欄位。 | 新增大型函式時沿用矩陣格式補直接契約測試。 |
| ✅ | TD-11 | runtime production code 無 `worker` 同名宣告。 | guardrail 持續防回歸。 |
| ✅ | TD-12 | TTL 集中、主要 cache bounded eviction 與 in-flight cleanup 已有證據。 | 新 cache 必須有 TTL/cap。 |
| ✅ | TD-13 | H-03～H-08 已完成 runtime broad catch 的 fallback／API error logging 對帳，以及 H-08 範圍 167 個 broad catch 的現況 inventory；regression／工具腳本均有結構化失敗結果、明確退路或最低限度診斷 context，`portable_check.py` health retry 另有 logging contract test。`.gitattributes` 已建立。 | 新增 broad catch 時保留可判斷 fallback、context 或 contract test；不大規模改寫。 |
| ✅ | TD-14 | CSS 已切為 7 個 split，並有資源量測；未進行 bundling。 | 效能打包歸 TD-18。 |
| 🟡 | TD-15 | 第一輪死碼/失效控制項與第二輪 5 條 JS observe-list 死鏈已完成；CSS dynamic/grouped candidates 仍保留待後續重新核實。 | 只依三方證據逐項處理。 |
| ✅ | TD-16 | 法人歷史資料不足時已停止無限遞迴，94 條互動護欄通過。 | 僅維護。 |
| ✅ | TD-17 | offline fixture、live health 分類與外部錯誤標籤已完成。 | 新端點沿用 offline/live 分離。 |
| ✅ | TD-18 | H-06 評估、H-10 shadow／canary、Terser minify、版本化 immutable cache、Service Worker cache version 與 21 頁 HTML／assets 切換均已完成；CSP 維持原值，部署入口未變更。 | 維持 classic slices／未壓縮 bundle 作為回退基線；CSS 合併、preload 與其他部署策略另行評估。 |
| ✅ | TD-19 | H-07 已完成 44/44 endpoint required-key policy、offline／quick verifier、live/full rollout 與錯誤分類；`schema_diff()` 原有 key 缺失盲區已由獨立 presence check 補上。 | 新 endpoint 依 44/44 policy coverage 規則增補 required／not-required 理由；維持 external failure 與合法空資料分類。 |
| ✅ | TD-20 | existing database/clean package integrity 已通過。 | 交付流程持續驗證。 |
| ✅ | TD-21 | route scan 已支援 Blueprint，49 routes 可被正確掃描。 | 僅維護。 |
| ✅ | TD-22 | portable package 缺 regression 目錄時可正確 SKIP，不誤報 FAIL。 | 維持 skip/fail 區分。 |
| ✅ | TD-23 | import app 不再初始化 SQLite；啟動/API request lifecycle 初始化已驗證。 | 不恢復 module import side effect。 |
| ✅ | TD-24 | 測試 cache、pycache 與 DB side-effect 已隔離並以 hash 驗證。 | 新測試沿用 temporary resource。 |

## 3. 新的執行順序

### H-01：前端大型 render function 第一小批次（P2，可選；✅ 2026-08-30 完成）

目標：降低 classic-script 共享全域作用域下的單函式複雜度，不改變頁面輸出、事件順序或 API。

範圍：

- 先處理 `renderStockDetail` 或 `renderTechnicalChart` 其中一個，不同時處理兩個。
- 優先抽出同一 closure 內的 markup、資料整理或事件綁定 helper，避免新增 global symbol。
- 保留現有 script 載入順序、`escapeHtml` 單一來源與所有 interaction manifest。

驗收：`node --check`、unit、security、e2e、frontend compare、interaction compare、full verify 全部通過；不得以更新 screenshot 掩蓋差異。

回退：單一函式一個 commit；若有畫面或互動差異，只回退該函式 commit。

執行結果（2026-08-30）：

- 本批次選定 `js/charts.js` 的 `renderTechnicalChart`，未同時處理 `renderStockDetail`。
- 在函式內抽出 overlay markup、hover zones、combined indicator panel config 與 chart markup helper；未新增 global symbol，未改變 script 載入順序、API、`escapeHtml` 或 interaction manifest。
- `node --check js/charts.js`、126 個 unit tests、16 項 security guardrail、`E2E_SMOKE_OK` 全部通過。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：live/cached API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- 未更新任何 screenshot、API 或 interaction baseline。第一次受限環境執行的 external resource error 已依規範分類，並以允許外部連線的同一命令重跑確認為環境問題而非程式碼回歸。

### H-02：TD-10 top-10 coverage 完整化（P2）

目標：把「已有副產品測試」與「專門 contract test」分開，完成 top-10 大型函式矩陣。

工作：

- 建立函式、fixture、成功/空資料/上游失敗、固定輸出欄位與測試名稱的矩陣。
- 補齊尚未有直接 contract test 的 builder/fetcher；沿用 `tests/fixtures/td10_large_function_contracts.json`，不把 live API 當 golden source。
- 每完成一組，先跑六層驗證再進下一組。

執行結果（2026-08-30）：

- 完成 `tests/fixtures/td10_large_function_contracts.json` 的 10 函式矩陣：`build_stock_detail`、`build_analysis_sections`、`build_sector_fund_flow`、`build_site_data`、`build_news`、`build_live_sector_site_data`、`fetch_yahoo_margin_trading`、`build_taifex_open_interest_item`、`build_taifex_stock_derivative_aggregate_item`、`build_chip_summary`。
- 矩陣固定記錄 fixture、成功測試名稱、空資料/上游失敗測試名稱（適用時）與 required output keys；新增測試均使用 mock/fixture，不把 live API 當 golden source。
- 新增直接契約測試涵蓋 `build_stock_detail`、`build_sector_fund_flow`、TAIFEX open-interest/aggregate、`build_chip_summary`，並補齊空資料與上游失敗 fallback/assertion。
- `python -m py_compile builders.py fetchers.py test_derivatives_platform.py` 通過；新增測試 16/16 通過，完整 unit 共 142 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：live/cached API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- 未更新 screenshot、API、frontend manifest 或 interaction baseline；未修改 production code。

### H-03：TD-13 broad exception 與行尾治理（P3）

目標：完成靜默例外盤點，讓每個保留的 broad catch 都有可判斷的 fallback、log context 或明確理由。

工作：

- 只處理未有記錄或無法辨識 fallback 的 catch；已經有 `warning`/`exception` 的不順手重寫。
- 先產生靜態盤點，再按模組各一小批處理。
- 確認 `.gitattributes` 對新增/修改 Python 檔案的行尾效果；不以整檔換行改寫製造大型 diff。

執行結果（2026-08-30，H-03-01）：

- 靜態盤點記錄：`builders.py` 有 61 個 `except Exception`、其中 28 個未直接呼叫 logging；`fetchers.py` 有 46 個、其中 19 個未直接呼叫 logging；`parsers.py` 有 6 個、其中 4 個未直接呼叫 logging。透過共用 `api_exception_response` 記錄的 route handler 另行辨識，未列為本子批次。
- 本子批次只處理 `build_stock_detail` 的 3 個歷史資料 catch：quick mode 的 Yahoo history、一般模式的 Yahoo history，以及 TWSE history fallback；新增 stock code、失敗情境與 fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本空列表與後續 generated fallback 行為。
- `test_derivatives_platform.py` 的 upstream-failure contract test 已驗證 quick-mode logging context；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 142 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次 `builders.py` 無 CRLF，未進行整檔換行改寫，未更新任何 baseline。TD-13 保持部分完成，後續仍從其他模組/子批次繼續。

執行結果（2026-08-30，H-03-02）：

- 處理 `fetchers.py` Nasdaq company supplement 責任範圍的 5 個 fallback catch：`fetch_nasdaq_company_profile`、`fetch_nasdaq_company_financials`、`fetch_nasdaq_company_institutional_holdings`、`fetch_nasdaq_company_insider_trades`，以及 `fetch_nasdaq_us_supplement` 的 fan-out future fallback。
- 新增 symbol/key、資料來源責任與 empty-object fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`；保留原本 `{}` 回傳與 fan-out 結果結構，不改 API 行為。
- 新增 2 個 logging contract tests；`python -m py_compile fetchers.py test_derivatives_platform.py` 通過；完整 unit 共 144 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-09）：

- 處理 `fetchers.py` `fetch_nasdaq_quote_endpoint` 的 1 個 asset-class retry fallback catch；補上 symbol、endpoint、asset class 與 retry 意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本嘗試下一個 asset class 的行為。
- 新增 1 個 logging contract test；`python -m py_compile fetchers.py test_derivatives_platform.py` 通過；完整 unit 共 157 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-10）：

- 處理 `fetchers.py` `fetch_us_treasury_yield_curve` 的 1 個 fallback catch；補上資料來源與 empty-yield-curve fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本 `{}` 回傳行為。
- 新增 1 個 logging contract test；`python -m py_compile fetchers.py test_derivatives_platform.py` 通過；完整 unit 共 158 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-11）：

- 處理 `builders.py` `build_institution_trend` 回溯日期資料抓取的 1 個 fallback catch；補上日期、資料來源與跳過失敗日期意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本略過該日並繼續回溯的行為。
- 新增 1 個 logging contract test；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 159 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-12）：

- 處理 `fetchers.py` `fetch_market_macro_factors` 五路並行資料彙整的 1 個 per-factor fallback catch；補上 factor key、market date 與跳過失敗因子意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本將失敗因子排除並繼續彙整其他結果的行為。
- 新增 1 個 logging contract test；`python -m py_compile fetchers.py test_derivatives_platform.py` 通過；完整 unit 共 160 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-13）：

- 處理 `fetchers.py` `collect_futures_until_deadline` 已完成 future 的 1 個 fallback catch；補上 future key、empty list/object fallback 與例外堆疊的 `LOGGER.debug(..., exc_info=exc)`，保留原本依 key 寫入空列表或空物件的行為；pending timeout 分支未納入本批次。
- 新增 1 個 logging contract test；`python -m py_compile fetchers.py test_derivatives_platform.py` 通過；完整 unit 共 161 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-14）：

- 處理 `builders.py` `estimate_us_beta` SPY benchmark 抓取的 1 個 fallback catch；補上 symbol、benchmark 與 unavailable beta fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本回傳 `None` 的行為。
- 新增 1 個 logging contract test；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 162 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-15）：

- 處理 `builders.py` `build_us_market_symbol_detail` 長期歷史 enrichment 的 1 個 fallback catch；補上 symbol、資料來源與保留既有 series 意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本忽略 5y history 失敗並使用既有 series 的行為。
- 新增 1 個 logging contract test；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 163 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-16）：

- 處理 `builders.py` `build_us_market_symbol_detail` 三路 component fan-out（summary/news/Nasdaq supplement）共用的 1 個 fallback catch；補上 symbol、component key、分支 fallback 與例外堆疊的 `LOGGER.debug(..., exc_info=exc)`，保留各分支原本空 summary、空 news list、空 Nasdaq supplement 的行為。
- 新增 1 個 logging contract test；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 164 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-17）：

- 處理 `builders.py` `build_site_data` 啟動階段的 3 個 source bootstrap fallback catch：TPEx mainboard quotes、Yahoo TPEx ETF mapping 與 Yahoo sector catalog；補上 market date、資料來源與空資料 fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本空列表／空 mapping 行為。
- 新增 1 個 logging contract test；因 mock 數量較多改以 `ExitStack` 管理測試 scope；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 165 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-18）：

- 處理 `builders.py` `build_site_data` TPEx/興櫃 highlight 與統計資料 fallback 的 3 個 catch；補上 market date、資料來源、空 highlight/空 statistics fallback 與例外堆疊的 `LOGGER.debug(..., exc_info=exc)`，保留原本失敗時使用空列表的行為。
- 新增 1 個 logging contract test；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 166 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-19）：

- 處理 `builders.py` `build_site_data` same-day activity 與 intraday 的 2 個 fallback catch；補上 market date、資料來源與無 activity date／無 intraday payload fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`，保留原本 activity/intraday 失敗時的回退值。
- 新增 1 個 logging contract test；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 167 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-20）：

- 處理 `builders.py` `build_site_data` 市場補充資料 fallback 的 3 個 catch：波動率、國際指數與宏觀因子；補上 market date、資料來源、unavailable/empty fallback 與例外堆疊的 `LOGGER.debug(..., exc_info=exc)`，保留原本 `None`、空列表與空 mapping 行為。
- 新增 1 個 logging contract test；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 168 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-21）：

- 處理 `builders.py` 剩餘 2 個未直接記錄的 broad catch：`build_global_market_item` 的 Yahoo chart retry，以及 `build_live_sector_site_data` 的 news fallback；補上 symbol、candidate、attempt、market date、fallback 意圖與例外堆疊的 `LOGGER.debug(..., exc_info=exc)`，保留原本 retry 與空 news 行為。
- 新增 2 個 logging contract tests；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 170 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。builders.py 本輪未直接記錄的 broad catch 已完成，TD-13 仍有 fetchers/parsers 例外待後續子批次處理。

執行結果（2026-08-30，H-03-22）：

- 處理 fetchers/parsers 各 1 個未直接記錄的 broad catch：`fetch_tdcc_holding_distribution_text` 的 TDCC retry，以及標記為 DEADCODE-CANDIDATE 的 `sync_yahoo_quotes` future fallback；補上 URL、attempt、stock index/code、fallback 意圖與例外堆疊的 logging context，保留原本重試後 re-raise 與保留原 stock 行為，未刪除候選死碼。
- 新增 2 個 logging contract tests；`python -m py_compile fetchers.py parsers.py test_derivatives_platform.py` 通過；完整 unit 共 172 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py`、`parsers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍有 fetchers/parsers 其他例外待後續子批次處理。

執行結果（2026-08-30，H-03-23）：

- 處理 runtime production 中 4 個已有 fallback 但未直接記錄的 broad catch：`app.py` `apply_yahoo_quote` 的 TPEx 盤中報價、`routes_twse.py` sector chart 的 history/benchmark future，以及 institutional history seed history；補上 stock/symbol、market、code、market date 與 fallback 意圖的 logging context，保留原本回傳原 stock、空 chart 與空 rows 行為。
- 新增 3 個 logging contract tests；`python -m py_compile app.py routes_twse.py test_derivatives_platform.py` 通過；完整 unit 共 175 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `app.py`、`routes_twse.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 runtime production broad catch 已具備 logging 或共用 API error response，工具腳本例外不納入本輪。

執行結果（2026-08-30，H-03-05）：

- 處理 `builders.py` sector-history 責任範圍的 5 個 fallback catch：市場快照、活動資料、快照 future、weighted index Yahoo history，以及 weighted index TWSE 月度 history。
- 新增日期/月度與 fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`；保留原本跳過 snapshot、空活動資料、Yahoo/TWSE fallback 與跳過月份行為，不改 API 結構。
- 新增 3 個 logging contract tests；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 151 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-08）：

- 處理 `fetchers.py` `fetch_yahoo_class_quote_pages` 的 1 個 pagination future fallback catch；補上 offset、URL 與跳過頁面的 `LOGGER.debug(..., exc_info=exc)`，保留原本略過失敗分頁、使用其餘頁面結果的行為。
- 新增 1 個 logging contract test；`python -m py_compile fetchers.py test_derivatives_platform.py` 通過；完整 unit 共 156 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-07）：

- 處理 `builders.py` Yahoo class quote/group 責任範圍的 3 個 fallback catch：primary class page、pagination，以及 sector group worker。
- 補上 prefix/url、group key 與 fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`；保留原本空 cards、使用 primary rows 與空 group 行為，不改 API 結構。
- 新增 2 個 logging contract tests；`python -m py_compile builders.py test_derivatives_platform.py` 通過；完整 unit 共 155 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `builders.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-06）：

- 處理 `fetchers.py` 國際市場指數/spot snapshot 責任範圍的 3 個 fallback catch：Yahoo fallback symbol 嘗試、國際指數 item catalog fallback，以及 Yahoo spot unavailable snapshot。
- 補上 key/symbol 與 fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`；國際指數 future 改以 future-to-spec 對照提供失敗 context，保留原本 fallback item 與 unavailable snapshot 行為，不改 API 結構。
- 新增 2 個 logging contract tests；`python -m py_compile fetchers.py test_derivatives_platform.py` 通過；完整 unit 共 153 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-03）：

- 處理 `parsers.py` `refresh_tpex_cache` 的 3 個 fallback catch：Yahoo TPEx ETF mapping、Yahoo OTC class cards、Yahoo emerging class cards；補上 fallback 意圖與 `exc_info` debug context，保留原本空 mapping/空 highlights 行為。
- 新增 1 個 logging contract test，驗證三種上游失敗情境；`python -m py_compile parsers.py test_derivatives_platform.py` 通過；完整 unit 共 145 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `parsers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-03-04）：

- 處理 `fetchers.py` 台股歷史/法人資料責任範圍的 5 個 fallback catch：估值歷史 future、單日法人 retry、法人歷史 future、月度股價歷史，以及近期交易月度資料。
- 新增 stock code、日期/月度、retry attempt 與 fallback 意圖的 `LOGGER.debug(..., exc_info=exc)`；保留原本跳過 point/record、retry/re-raise 與空月度資料行為，不改 API 結構。
- 新增 3 個 logging contract tests；`python -m py_compile fetchers.py test_derivatives_platform.py` 通過；完整 unit 共 148 tests PASS；security guardrail 16/16 PASS；`E2E_SMOKE_OK`。
- `regression/verify_against_baseline.py --full` 在允許外部連線環境下回傳 `VERIFY_OK`：API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git check-attr` 確認 `fetchers.py` 等 Python 檔套用 `text: auto`、`eol: lf`；本次未進行整檔換行改寫，未更新任何 baseline。TD-13 仍為部分完成，剩餘例外治理另列後續子批次。

執行結果（2026-08-30，H-04-01）：

- 依 TD-15 三方證據逐組核實第一組 backtest CSS 候選；目前實際程式庫中 `backtest-benchmark-card`、`backtest-factor-list`、`backtest-learning-summary` 已無規則，`backtest-signal-list` 僅出現在含有效 selector 的 media query，均不作整段刪除。
- 確認 `.backtest-signal.is-bullish` 與 `.backtest-signal.is-bearish` 為純規則區塊，且全 repo 無 HTML/JS/Python 引用、無動態 class 生成或頁面互動依賴；移除該兩個規則，未變更有效 selector 群組、HTML、JS、API 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-02）：

- 重新核實 backtest/chart 小型候選群：backtest 其餘分類項目目前沒有可刪的實體純規則；`chart-stack`、`chart-area`、`chart-points circle` 實際位於未列入靜態白名單、未被頁面載入的 `assets/styles.css`。
- 確認三個 chart 規則全 repo 無 HTML/JS/Python 引用、無動態 class 生成、無 grouped selector 或頁面互動依賴；移除 `assets/styles.css` 三個純規則區塊，未變更目前 21 頁使用的 `split-*.css`、HTML、JS、API 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-03）：

- 重新核實 split-01 `class-hero` 候選組：`class-hero-value-row` 與 `class-hero-metrics` 的實際規則目前只存在於未列入靜態白名單、未被頁面載入的 `assets/styles.css`，分類表中的舊 split 行號不作為現況依據。
- 確認兩個候選及其子元素／mobile 純規則共 5 個區塊，全 repo 無 HTML/JS/Python 引用、無動態 class 生成、無 grouped selector 或頁面互動依賴；移除該 5 個純規則，未變更目前 21 頁使用的 `split-*.css`、HTML、JS、API 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-04）：

- 重新核實 split-03 chip/margin/US-sector 候選，選定 `chip-summary-card` 小組；`chip-summary-grid` 因與有效 `.analysis-card-chips` selector 綁定而保留，margin 與 US-sector 狀態樣式不納入本批次。
- 確認 `chip-summary-card.is-up`、`chip-summary-card.is-down` 及兩個 head badge 純規則共 4 個區塊，全 repo 無 HTML/JS/Python 引用、無動態 class 生成或頁面互動依賴；移除該 4 個規則，未變更其他有效 chip、margin、US-sector selector 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-05）：

- 重新核實 futures snapshot 候選，選定 `futures-timeframe-chip` 小組；snapshot rank/bar 相關 selector 含 active 狀態規則，留待後續批次重新確認，不混入本批次。
- 確認 `.futures-timeframe-chip.is-disabled` 為唯一純規則，且全 repo 無 HTML/JS/Python 引用或動態 class 生成；移除該規則，未變更 futures snapshot 的有效結構、active 狀態樣式或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-06）：

- 重新核實 US architecture/search/ETF/watchlist 候選，選定 `us-architecture-node` 小組；US ETF/search 多數候選屬含有效 selector 的 grouped rule，本批次保留不動。
- 確認 `us-architecture-node` 的 7 個色彩純規則全 repo 無 HTML/JS/Python 引用、無動態 class 生成或頁面互動依賴；移除該 7 個規則，未變更其他 US architecture、ETF、search、watchlist selector 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-07）：

- 重新核實 options heatmap/decision/region 候選，選定 `options-heat-cell` 小組；`options-heatmap-layout` 因屬 grouped media selector、`options-heatmap-side` 因仍有相關樣式，均保留不動。
- 確認 `options-heat-cell.is-call` 與 `options-heat-cell.is-put` 為 2 個純規則，且全 repo 無 HTML/JS/Python 引用或動態 class 生成；移除該 2 個規則，未變更 options heatmap 的有效 selector 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-08）：

- 重新核實 options market-chain 候選，`options-market-chain-data-wrap` 與 `options-market-chain-row-button` 已無實體規則；目前仍被 JS 使用的 selector/categories/products/data-panel/summary selector 均保留。
- 確認 `.options-market-chain-data-table tr.is-active td` 為唯一純規則，且全 repo 無 HTML/JS/Python 引用或動態 class 生成；移除該規則，未變更 options market-chain 有效 selector 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-09）：

- 重新核實 US search/ETF/watchlist 候選，選定 `us-etf-category-filters` 小組；`us-etf-detail-badges`、`us-etf-entry-card/tags` 因牽涉有效 grouped selector，保留不動。
- 確認 `.us-etf-category-filters .range-button` 與其 `small` 子元素規則共 2 個純規則，且候選根 class 全 repo 無 HTML/JS/Python 引用或動態生成；移除該 2 個規則，未變更其他 ETF/search/watchlist selector 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-10）：

- 重新核實 US sector 候選，選定 `us-sector-plain-result` 小組；其餘 focus-line、relative-band、alpha-area、chart-stat-strip、end-label 等狀態規則留待後續批次重新確認。
- 確認 `.us-sector-plain-result.is-ahead` 與 `.us-sector-plain-result.is-behind` 為 2 個純規則，且全 repo 無 HTML/JS/Python 引用或動態 class 生成；移除該 2 個規則，未變更其他 US sector selector 或 runtime 行為。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過；未更新任何 baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-11）：

- 完成 TD-15 第一條 institutional trade 疑似死碼呼叫鏈重評；鏈內 5 個函式 `buildTwseInstitutionTradeUrl`、`fetchClientInstitutionalTradeForDate`、`fetchClientInstitutionalTradeHistory`、`buildClientInstitutionalTradeRecord`、`buildClientInstitutionalTradeSummary` 均無產品入口、頁面入口或 interaction manifest 依賴，僅有鏈內呼叫。
- 移除上述 5 個函式及 `js/legacy-unclassified.js` 對應觀察標記；保留全站共用 `fetchWithTimeout`。同步更新 `regression/baseline/frontend/global_symbols.json` 移除 5 個預期 global symbol，未修改驗證邏輯；其餘 4 條疑似死碼鏈仍維持觀察。
- `node --check js/api.js`、`node --check js/legacy-unclassified.js` 通過；`python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare、global-symbol stability 與 security 全部通過；未更新 screenshot/API/interaction baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-12）：

- 完成 TD-15 第二條 weighted VIX comparison 疑似死碼呼叫鏈重評；`buildWeightedVixComparisonModel` 與 `renderWeightedVixComparisonChart` 僅有鏈內呼叫關係，無產品入口、頁面入口或 interaction manifest 依賴，且鏈外無 runtime 呼叫。
- 移除上述 2 個函式及 `js/legacy-unclassified.js` 對應觀察標記；同步更新 `regression/baseline/frontend/global_symbols.json` 移除 2 個預期 global symbol，未修改驗證邏輯；其餘 3 條疑似死碼鏈仍維持觀察。
- `node --check js/charts.js`、`node --check js/legacy-unclassified.js` 通過；`python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare、global-symbol stability 與 security 全部通過；未更新 screenshot/API/interaction baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-13）：

- 完成 TD-15 第三條 futures mini sparkline 疑似死碼呼叫鏈重評；`getFuturesFiniteVisualSeries` 與 `renderFuturesMiniSparkline` 僅有鏈內呼叫關係，無產品入口、頁面入口或 interaction manifest 依賴，且鏈外無 runtime 呼叫。
- 移除上述 2 個函式及 `js/legacy-unclassified.js` 對應觀察標記；同步更新 `regression/baseline/frontend/global_symbols.json` 移除 2 個預期 global symbol，並將 `regression/baseline/escapehtml_baseline.json` 的呼叫次數由 1905 更新為 1904；未修改驗證邏輯。其餘 2 條疑似死碼鏈仍維持觀察。
- `node --check js/legacy-unclassified.js`、`node --check js/page-global-market-futures.js` 通過；`python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：首次檢出 escapeHtml frozen baseline 須同步，更新後重跑為 `VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare、global-symbol stability 與 security 全部通過；未更新 screenshot/API/interaction baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-14）：

- 完成 TD-15 第四條 futures indicator switch 疑似死碼呼叫鏈重評；`renderFuturesIndicatorPlot` 與 `renderFuturesIndicatorSwitchChart` 僅有鏈內呼叫關係，無產品入口、頁面入口或 interaction manifest 依賴，且鏈外無 runtime 呼叫。
- 移除上述 2 個函式及 `js/legacy-unclassified.js` 對應觀察標記；同步更新 `regression/baseline/frontend/global_symbols.json` 移除 2 個預期 global symbol，並將 `regression/baseline/escapehtml_baseline.json` 的呼叫次數由 1904 更新為 1895；未修改驗證邏輯。其餘 1 條疑似死碼鏈仍維持觀察。
- `node --check js/legacy-unclassified.js`、`node --check js/page-global-market-futures.js` 通過；`python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare、global-symbol stability 與 security 全部通過；未更新 screenshot/API/interaction baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-04-15）：

- 完成 TD-15 第五條 US watchlist 疑似死碼呼叫鏈重評；`loadUsWatchlistSymbol` 與 `renderUsWatchlistSearchResults` 無產品入口、頁面入口或 interaction manifest 依賴，且鏈外無 runtime 呼叫；`initUsWatchlistPage` 不在此鏈上。
- 移除上述 2 個函式及 `js/legacy-unclassified.js` 對應觀察標記；同步更新 `regression/baseline/frontend/global_symbols.json` 移除 2 個預期 global symbol，並將 `regression/baseline/escapehtml_baseline.json` 的呼叫次數由 1895 更新為 1887；未修改驗證邏輯。5 條 JS 疑似死碼鏈全部完成。
- `node --check js/legacy-unclassified.js`、`node --check js/page-us.js` 通過；`python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare、global-symbol stability 與 security 全部通過；未更新 screenshot/API/interaction baseline。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

### H-04：TD-15 第二輪死碼候選（P3，需逐項核准）

入口：`docs/td15_observe_list.md`、`docs/td15_css_classification.md`。

只有在 AST 不可達、runtime 無呼叫、頁面互動無依賴三方證據一致時才刪除；不確定項目保留並記錄理由。每次只處理一組候選。

### H-05：TD-03 長期共享狀態設計（P2，先設計後實作）

先完成容量、migration、故障模式、部署拓撲與回退方案評估，再決定 Redis/SQLite。這是行為與部署變更型工作，沒有設計確認前不得直接切換。

規劃結果（2026-08-30，僅設計，未執行切換）：

- 現況基線：Render 單一 web service 使用持久磁碟 `/var/data`；`Procfile` 與 `render.yaml` 均固定 `gunicorn --workers 1 --threads 4`。`API_RATE_LIMIT_STATE`、`cache_data`、`cache_flights`、`penny_sector_recommendation_cache` 與 `taifex_options_chain_inflight` 都是 process-local；`twse-cache.json` 與 derivatives SQLite 是持久資料，不應直接當作多 worker 的協調層。
- 建議目標：Redis 只承載可過期、需跨 worker 一致的 ephemeral state：rate-limit sliding window、cache-flight／options in-flight lease、penny recommendation cache，以及 cache 的 L2 共享讀取。每個 worker 可保留 bounded local L1 cache；Redis 不取代 derivatives SQLite 的持久資料。
- SQLite 邊界：H-05 不改資料表、不搬移正式 SQLite、不把 SQLite 放進跨 instance 寫入路徑。若未來要水平擴展到多 instance，另立 managed PostgreSQL migration 工單；單 instance 前仍保留目前 `/var/data/derivatives-platform.sqlite3`。
- Disk cache 邊界：`twse-cache.json` 只作 cold-start/warm-start fallback；共享模式下不把檔案鎖當成跨 worker lock，也不以本機 JSON 作為 rate-limit 或 single-flight 的權威來源。
- 背景更新：新增 Redis lease（token、TTL、續租與 finally release）確保同一時間只有一個背景 updater；lease 遺失時允許安全重試，但不得留下永久鎖。現行 60 秒週期與 cache TTL 先維持不變。

容量與觀測計畫：

- 目前 rate limit 為每 client 每 60 秒 120 次、process-local identity cap 10,000；理論上限以 `active_clients × window_hits` 估算，Redis sizing 另加 cache payload bytes、lease 數量與至少 30% headroom，不以單次測試的低流量數字估算。
- 目前 13 個 per-key cache bucket 的 cap 合計 8,800 entries；正式 sizing 前量測 p50/p95/p99 payload bytes、active keys、hit/miss、TTL expiration、eviction、Redis command latency、lock wait/timeout、background lease takeover 與 error rate。
- 先建立 24 小時 baseline dashboard／log 欄位，再以壓測產出單 worker、雙 worker、Redis latency、Redis unavailable 四組比較；沒有容量與故障數據前不開多 worker。

Migration 分批：

1. `H-05-01` 需求與觀測：只補 metrics、state inventory、雙 worker test harness，不改 production 行為。
2. `H-05-02` shared-state adapter：定義 rate-limit、TTL cache、lease 的介面；以 in-memory fake 與 Redis contract test 覆蓋正常、超時、斷線、重啟與 stale lease，不先切預設值。
3. `H-05-03` rate-limit：先 shadow compare，再啟用 Redis atomic sliding-window；429、`Retry-After`、health exempt 與 client cap 行為須保持契約。
4. `H-05-04` single-flight：先導入 cache-flight／options in-flight lease，token fencing、TTL、wait timeout 仍維持 120 秒語意；Redis 失效時以明確降級策略避免永久等待。
5. `H-05-05` cache L2：按 bucket 逐組導入 Redis TTL 與 version namespace，保留 bounded local L1 及 disk warm-start，不一次搬移所有 snapshot。
6. `H-05-06` background scheduler：導入 updater lease，確認多 worker 只有一個 refresh owner，並驗證失敗後可接管。
7. `H-05-07` staging/canary：雙 worker 壓測、故障注入與人工檢查通過後，才提出部署設定變更；每批次獨立驗證、可單批回退。

故障策略（需確認後才實作）：

- Redis unavailable：在 shared mode 啟用前維持 `--workers 1`；啟動檢查若要求 shared mode 卻沒有 Redis，建議 fail-fast 並保留明確 health/log 訊息。cache/single-flight 可降級為 local path，但 rate-limit 建議對受保護 API fail-closed，避免多 worker 下悄悄放寬安全限制。
- Redis timeout／partition：所有操作設短 timeout，禁止 request 無限等待；lease 使用 owner token 與 TTL，不能只用 `DEL key` 釋放他人 lease。重試與 fallback 必須記錄原因及 request key。
- Redis restart：使用 namespace/version、TTL 與 idempotent write；短暫遺失只造成 cache miss／重新抓取，不得遺失 SQLite 持久資料。
- SQLite lock／disk failure：維持現有 transaction、backup 與單 instance 限制；H-05 不以 Redis 作為 SQLite durable write-ahead log。

部署拓撲與回退：

- 現階段維持 Render 單 service、持久磁碟、`app:app`、1 worker；設計完成不代表可以修改 `Procfile`、`render.yaml` 或 worker 數。
- 目標拓撲為同區域 managed Redis + web service；若仍使用單 instance，可先只導入 rate-limit／single-flight 的 shared adapter，不急著改 worker 數。
- 每個實作批次以 feature flag／環境變數控制；回退順序為關閉 shared mode → 恢復單 worker → 回退該批次 adapter。回退不得刪除 Redis 以外的正式資料，也不得改寫既有 SQLite/cache 檔。

H-05 的設計驗收與待確認事項：

- 需確認 Redis 供應商、可接受月成本／延遲、rate-limit Redis 失效時採 fail-closed 的業務可接受度，以及是否要將 cache L2 納入第一階段；H-05-02 僅完成未接線的 adapter／contract，未替代這些決策。
- H-05-01 僅限觀測與測試基礎，可在不改 production 行為的前提下執行；H-05-02 之後的新增 dependency、部署設定、worker 數或 runtime 行為變更，必須先確認方案、另行說明影響與 revert 路徑並逐批跑完整驗證。

執行結果（2026-08-30，H-05-01）：

- 完成 TD-03 state inventory 與雙 process probe；新增 `regression/td03_shared_state_probe.py` 與 `docs/td03_shared_state_inventory.md`。probe 僅觀測 `security.py`、`cache.py`、`market_config.py`，未 import `app.py`、未啟動 Flask、未讀寫正式 SQLite 或 `twse-cache.json`。
- probe 確認兩個獨立 process 各自接受第一個 rate-limit hit、reader 讀不到 writer 的 memory cache、兩個 process 都能成為同一 cache-flight key leader；現況 process-local 邊界已取得可重現證據。inventory 記錄 5 類 process-local state、7 個 lock、rate-limit 60 秒／120 次／10,000 clients、13 個 per-key bucket／8,800 entries、cache-flight wait 120 秒與雙 worker 部署設定。
- `python -m py_compile regression/td03_shared_state_probe.py` 通過；`python regression/td03_shared_state_probe.py`：`TD03_SHARED_STATE_PROBE_OK`。
- `python -m unittest test_derivatives_platform.py`：175 tests PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`；API、21 頁 frontend compare、94 條 interaction compare、global-symbol stability、escapeHtml 與 security 全部通過；未更新任何 baseline，未修改 production runtime、Redis／SQLite 或部署設定。外部 TAIFEX/Yahoo 的 WinError 10013 僅為既有環境連線警告。

執行結果（2026-08-30，H-05-02）：

- 新增 `shared_state.py` 與 `test_shared_state.py`，定義 rate-limit、TTL cache、owner-token lease 的 shared-state adapter 契約；`InMemorySharedStateAdapter` 提供 deterministic fake，`RedisSharedStateAdapter` 採 optional injected redis-py-compatible client，未新增 Redis dependency。
- contract tests 覆蓋正常 cache／rate-limit／lease、TTL、client cap、owner-token fencing、timeout、disconnect、stale lease 與 memory restart；`MARKET_PULSE_REDIS_URL` 未設定，因此實連 Redis contract test 正確略過，未宣稱 Redis server 已驗證。
- H-05-02 未修改 production import、runtime 行為、Procfile、render.yaml、worker 數、SQLite、`twse-cache.json` 或任何 baseline；H-05-03 才處理 rate-limit shadow 與 Redis runtime 接線。
- `python -m py_compile shared_state.py test_shared_state.py` 通過；`python -m unittest test_derivatives_platform.py test_shared_state.py`：179 tests（1 skipped）PASS；`python security_guardrail_check.py`：16/16 PASS；`python e2e_smoke.py`：`E2E_SMOKE_OK`；`python regression/verify_against_baseline.py --full`：`VERIFY_OK`。

執行結果（2026-08-31，H-05-03）：

- `security.py` 新增 `MARKET_PULSE_RATE_LIMIT_MODE=local|shadow|redis`；預設 `local` 完全沿用既有 process-local 行為。`shadow` 以 local 結果決策並記錄 shared 比對；`redis` 使用 H-05-02 的 atomic Lua sliding-window adapter 作為權威。
- shared mode 採 lazy Redis client、短 timeout 與首次 `ping`；Redis 未設定、缺少 optional package、timeout 或 disconnect 時，受保護 API 回 503 `RATE_LIMIT_BACKEND_UNAVAILABLE`，health endpoint 維持 exempt，確保 fail-closed。
- 新增測試覆蓋 shadow 比對、redis 結果與 fail-closed；`MARKET_PULSE_REDIS_URL` 未設定，實連 Redis contract test 略過，未宣稱 Redis server 已驗證。
- H-05-03 未修改 Procfile、render.yaml、requirements、worker 數、SQLite、`twse-cache.json` 或 baseline；`python -m py_compile security.py shared_state.py test_shared_state.py` 通過；`python -m unittest test_derivatives_platform.py test_shared_state.py`：182 tests（1 skipped）PASS；security 16/16、E2E `E2E_SMOKE_OK`、full verify `VERIFY_OK`。

執行結果（2026-08-31，H-05-04）：

- `cache.py` 新增 `CacheFlightHandle` 與 `MARKET_PULSE_SINGLE_FLIGHT_MODE=local|redis`；generic cache-flight 與 TAIFEX options in-flight 均可使用 H-05-02 owner-token lease，預設 `local` 行為維持不變。
- shared mode 的 lease TTL／wait 上限為 120 秒；leader finish 以 owner token fencing 釋放，stale leader 不可釋放新 leader 的 lease。Redis claim／wait／release timeout 或 disconnect 時明確記錄並降級 local／重新檢查路徑，避免永久等待。
- 新增 `test_cache_shared_state.py`，覆蓋 local event、cross-process lease wait、stale owner fencing、backend failure fallback 與 options chain lease；未啟用 shared mode、未新增 Redis dependency、未修改 Procfile、render.yaml、worker 數、SQLite、`twse-cache.json` 或 baseline。
- `python -m py_compile cache.py fetchers.py shared_state.py test_cache_shared_state.py` 通過；`python -m unittest test_derivatives_platform.py test_shared_state.py test_cache_shared_state.py`：187 tests（1 skipped）PASS；security 16/16、E2E `E2E_SMOKE_OK`、full verify `VERIFY_OK`。

執行結果（2026-08-31，H-05-06）：

- `cache.py` 新增 `MARKET_PULSE_BACKGROUND_LEASE_MODE=local|redis` 與 `run_background_update_cycle()`；shared mode 下 background updater 需取得 `background-updater` lease 才執行兩段 refresh，預設 `local` 行為維持不變。
- lease TTL／refresh wait 語意為 120 秒，refresh 期間由 renew thread 續租，finally 以 owner token fencing release；其他 worker 持有時 skip，過期後可 takeover。
- Redis 未設定、timeout 或 disconnect 時 skip 該輪而非 local fallback，避免多 worker 同時 refresh；下一輪可重試，無永久 lock。未修改 Procfile、render.yaml、worker 數、requirements、SQLite、`twse-cache.json` 或 baseline。
- 新增測試覆蓋 local default、lease contention、release、renewal、expired takeover 與 backend failure；`python -m py_compile cache.py shared_state.py test_cache_shared_state.py` 通過；`python -m unittest test_derivatives_platform.py test_shared_state.py test_cache_shared_state.py`：197 tests（1 skipped）PASS；security 16/16、E2E `E2E_SMOKE_OK`、full verify `VERIFY_OK`。

執行結果（2026-08-31，H-05-05）：

- `cache.py` 新增 `MARKET_PULSE_CACHE_L2_MODE=local|redis`；預設 `local` 不建立 Redis client。L2 先納入 6 個 JSON-safe accessor bucket：`live_search_dedup`、`us_options_chains`、`global_market_items`、`yahoo_tw_future_technical_candles`、`taifex_openapi_list`、`yahoo_tw_stock_resources`。
- read path 採 bounded local L1 → versioned Redis L2；L2 hit 回填 L1 並套用既有 bucket cap。write path 以呼叫端實際 TTL 寫入 L2，namespace 包含 `CACHE_VERSION`（`cache-v13`）。`external_text` 因同時含 binary payload 暫不納入。
- L2 miss、Redis timeout／disconnect、非 JSON payload 均明確降級 local，不阻斷既有抓取；未經 accessor 的 snapshot／direct `cache_data` 留待後續批次。未修改 Procfile、render.yaml、worker 數、requirements、SQLite、`twse-cache.json` 或 baseline。
- `python -m py_compile cache.py fetch_registry.py fetchers.py builders.py shared_state.py test_cache_shared_state.py` 通過；`python -m unittest test_derivatives_platform.py test_shared_state.py test_cache_shared_state.py`：191 tests（1 skipped）PASS；security 16/16、E2E `E2E_SMOKE_OK`、full verify `VERIFY_OK`。

執行結果（2026-08-31，H-05-07）：

- 新增 `regression/td03_shared_state_canary.py` 與 `test_td03_shared_state_canary.py`，完成 offline 雙 worker canary preflight；腳本不 import `app.py`、不啟動 Flask、不讀寫正式 SQLite 或 `twse-cache.json`。
- deployment gate 驗證 `Procfile` 與 `render.yaml` 均為 `--workers 1`；shared rate-limit、cache readback、lease contention／owner-token fencing／expired takeover、background scheduler 單 owner／接管與四類 backend fault injection 全部通過。
- `redis==8.1.0` 已安裝並寫入 `requirements.txt`；`MARKET_PULSE_REDIS_URL` 未配置，因此本批次未執行實連 Redis staging canary，明確保留為待環境配置事項。未修改 Procfile、render.yaml、worker 數、SQLite、`twse-cache.json` 或 baseline。
- `python -m py_compile regression/td03_shared_state_canary.py test_td03_shared_state_canary.py` 通過；`python regression/td03_shared_state_canary.py` 輸出 `TD03_SHARED_STATE_CANARY_OFFLINE_OK`；H-05 測試 199 tests（1 skipped）PASS，實連 Redis case 因 URL 未設定而略過。
- 依本次授權重新盤點，尚待處理的 L2 bucket 為 `external_text`、`global_markets`、`sector_charts`、`stock_details`、`taifex_options_chain`、`us_etf_center`、`yahoo_tw_option_chain`；provider 與 multi-worker rollout 也尚未執行。
- 目前環境仍缺少 `MARKET_PULSE_REDIS_URL` 與本機 Redis service；雖已補齊 client dependency，仍無法完成實連 canary。H-05 維持「部分完成」，不得改標為 100%。
- 使用者決定將實連 Redis staging、`MARKET_PULSE_REDIS_URL` 配置、7 個未納入 L2 bucket、provider 評估與 multi-worker rollout 封存暫緩；後續需重新授權且 staging 條件具備後，另立批次恢復。
- H-05 目前完成度：H-05-01～H-05-06 完整完成，H-05-07 完成 offline canary；以 7 個正式批次計算為 6/7（約 85.7%），目前授權範圍已完成，但整體不標示為 100%。

### H-06：TD-18 前端打包效能評估（P3，先評估）

先量測目前 15 個 JS slice、7 個 CSS split 的實際冷啟動量、快取命中與載入時間，產出成本/收益/回退報告；本項未核准前不引入 bundler、不改 CSP、不變更部署工具鏈。

規劃結果（2026-08-31，僅規劃，不執行程式或建置工具變更）：

- `H-06-01` 靜態資產盤點：固定記錄 15 個 classic-script JS slice、7 個 CSS split 的 bytes、gzip／brotli size、script 載入順序、頁面覆蓋範圍與共用全域符號；以 `docs/frontend_module_map.md` 為依賴邊界，不重排現有載入順序。
- `H-06-02` 冷啟動／快取量測：使用現有本機 frontend harness 對 21 頁執行 cold cache、warm cache、重整與代表性互動，收集 navigation timing、request count、asset bytes、first meaningful render proxy、cache hit／miss 與錯誤率；不更新 screenshot／interaction baseline。
- `H-06-03` 候選方案評估：比較維持 classic slices、HTTP/2／HTTP/3 下的 request 合併、保守 bundler、CSS 合併與 preload／cache policy；評估 global scope、CSP、source map、cache invalidation、debuggability、部署成本與回退複雜度，產出成本／收益矩陣。
- `H-06-04` canary／回退門檻：定義不改行為的 shadow build、單頁 canary、全頁 rollout gate 與 revert 路徑；任何 bundler、CSP、HTML script 標籤、`CACHE_VERSION` 或部署工具鏈變更均另立批次並先取得確認。

執行結果（2026-08-31，H-06-01）：

- 完成 15 個 classic-script JS slice 與 7 個 CSS split 的靜態資產盤點，固定記錄原始 bytes、gzip／Brotli bytes、21 頁覆蓋範圍與目前 script 載入順序；JS 合計 1,690,102／409,155／328,295 bytes，CSS 合計 419,743／59,730／51,090 bytes。
- 確認 21 頁均載入相同 15 個 slice，順序為 `state → core → api → shared-calc → render-shared → charts → stock-detail → page-home → page-us → page-global-market-futures → page-global-market-options → page-global-market-assethub → page-tw → legacy-unclassified → main`；`docs/frontend_module_map.md` 依賴邊界未變更。
- 現況 `regression/baseline/frontend/global_symbols.json` 記錄 895 個共享 classic-script 全域符號；未改 CSP、HTML script 標籤、`CACHE_VERSION`、部署工具鏈或任何 baseline。完整明細見 `docs/TD18_前端打包效能評估_2026-08-31.md`。
- `python -m py_compile app.py`、175 tests、security 16/16、`E2E_SMOKE_OK` 通過；第一次受限環境 full verify 的 TWSE 502／外部資源拒絕依規範分類為 `[外部問題]`，以允許外部連線環境重跑後 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。

執行結果（2026-08-31，H-06-02）：

- 新增只讀量測腳本 `regression/td18_frontend_measure.py`，沿用 `server_harness.py`、interaction HAR 與 `interaction_specs.py`；21 頁均完成 cold／warm／refresh 量測與一條代表性互動／render-only 檢查。
- 21 頁平均 navigation 為 cold／warm／refresh `142.8／148.3／217.1 ms`，first meaningful render proxy 為 `201.1／159.5／255.3 ms`；靜態資產 transfer 合計每輪 `44,551,367 bytes`，asset cache hit 三輪均為 0，符合現行 no-store 政策。
- 19 條代表性互動成功；`tw-Optional-stocks.html`、`us-watchlist.html` 各一條互動因空 holdings 無可點擊欄位，記為資料前置條件，不是產品回歸。完整 21 頁逐頁數據見 `docs/TD18_前端打包效能評估_2026-08-31.md`。
- `python -m py_compile regression/td18_frontend_measure.py`、175 tests、security 16/16、`E2E_SMOKE_OK` 通過；外部連線 full verify 回傳 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過；量測輸出寫入 temp JSON，未更新任何 baseline。

執行結果（2026-08-31，H-06-03）：

- 完成維持 classic slices、HTTP/2／HTTP/3 transport、保守 bundler、CSS 合併、preload／cache policy 五類候選的成本／收益／風險矩陣；未執行 bundler，未改 CSP、headers、HTML、部署工具鏈或 baseline。
- 依 H-06-02 實測，短期保留 classic slices 作為回退基線；中期推薦先做 deterministic shadow build，再進單頁 canary；CSS 合併與 cache policy 分開立項，避免 cascade 或部署 cache 變更混入 bundler 回歸原因。
- `python -m py_compile app.py`、175 tests、security 16/16、`E2E_SMOKE_OK` 通過；外部連線 full verify 回傳 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。

執行結果（2026-08-31，H-06-04）：

- 完成 deterministic shadow build、`index.html` 單頁 canary、全頁 rollout gate 與 classic-slice revert 路徑；明定 syntax／CSP／global symbol／pixel diff／DOM／interaction／API、效能 p50 與資產減量門檻。
- 明定首個 canary 為 `index.html`、第二候選為 `futures.html`；要求至少 5 次 cold／warm，p50 meaningful render 不得退化超過 10%，資產請求／bytes 不得增加，且需至少 15% 壓縮後資產減量或減少 6 個請求才進全頁 rollout。
- 未建立 bundler、未改 CSP、HTML script 標籤、`CACHE_VERSION`、部署工具鏈或 baseline；回退保留 classic assets 並要求回退後重跑 full verify。

H-06 驗收條件：完成 15 JS／7 CSS 的尺寸與載入順序基線、21 頁 cold／warm 數據、候選方案成本／收益／風險矩陣與可執行回退方案；未達驗收前不得引入 bundler 或修改 CSP／部署工具鏈。

### H-07：TD-19 required-key allowlist（P3，另批次）

目標：補上 `schema_diff()` 對「關鍵欄位整段消失」的盲區，讓必要欄位缺失被分類為 `[程式碼可能改動回應格式]`；保留既有 structure-only 對時變欄位、`null`／空資料與額外欄位的容忍範圍，不改產品 API 行為。

現況盤點（2026-08-31，僅規劃）：

- `regression/baseline/manifest.json` 目前有 44 個 endpoint，其中 42 個為 `structure_only`、2 個為 value compare；`regression/diffing.py::schema_diff()` 目前刻意忽略 dict key 的缺失／新增，TD-19 盲區仍存在。
- 工作樹目前可見一組未提交的 TD-19 草稿（`regression/verify_against_baseline.py` 的 `REQUIRED_KEY_PATHS` 與 `regression/test_offline_verifier.py`）；這只視為待審核素材，不視為 H-07 已完成，也不得在規劃批次直接覆蓋或提交。

執行結果（2026-08-31，H-07-01）：

- 完成 44/44 endpoint 的 required／optional 欄位政策盤點，涵蓋 manifest name、request path、baseline 型態、候選 required keys、合法空資料語意與暫不要求理由；產出 `docs/TD19_required_key_matrix_2026-08-31.md`。
- 盤點確認 42 個 endpoint 為 `structure_only`、2 個為 value compare；Yahoo 兩個現有 error-only fixture 暫不宣告成功型 required keys，列入 H-07-02 待補 fixture／分類事項。
- 本批只新增盤點文件，未修改 verifier、產品 API 或 baseline；既有工作樹 TD-19 草稿維持待審核狀態。
- `matrix_rows=44`、`python -m py_compile app.py regression/verify_against_baseline.py`、177 tests（含 TD-19 offline regression tests）、offline `--quick`、security 16/16、`E2E_SMOKE_OK` 通過；外部連線 full verify 回傳 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。

執行結果（2026-08-31，H-07-02）：

- 完成 allowlist schema、key presence／null／空資料語意、44/44 coverage policy、failure classification 順序與 12 個 mutation／設定錯誤測試案例設計；產出 `docs/TD19_required_key_design_2026-08-31.md`。
- 明確規定 external failure 先分類、required-key 僅檢查存在性、error-only endpoint 不得冒充成功契約；本批未擴充 verifier 或更新 baseline，工作樹既有 TD-19 草稿維持待審核。
- `design_test_cases=12`、`python -m py_compile app.py regression/verify_against_baseline.py`、177 tests、offline `--quick`、security 16/16、`E2E_SMOKE_OK` 通過；外部連線 full verify 回傳 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。

執行結果（2026-08-31，H-07-03）：

- 將 required-key policy 接入 offline fixture／`--quick` verifier；44/44 manifest endpoint 均有 policy coverage，支援 object dot-path，並在比對前區分 external failure、status／body 型態錯誤與 required key 缺失。
- 新增／補強 TD-19 offline regression tests：缺少頂層／巢狀 key、合法 `null`／空資料、external failure 優先、非 object body、unknown policy coverage；183 tests 通過，錯誤注入確認會回報 schema failure。
- `python -m py_compile app.py regression/verify_against_baseline.py regression/test_offline_verifier.py`、183 tests、offline `--quick`、security 16/16、`E2E_SMOKE_OK` 通過；外部連線 full verify 回傳 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。
- 未修改產品 API、CSP、部署設定或 golden baseline；變更限於 regression verifier、TD-19 專用測試與文件。

執行結果（2026-08-31，H-07-04）：

- 完成 live／full rollout 最終核對：44/44 policy coverage 通過，required-key check 同時覆蓋 offline／quick 與 live／full；未發現未分類 schema failure。
- `python -m py_compile app.py regression/verify_against_baseline.py regression/test_offline_verifier.py`、183 tests、offline `--quick`、security 16/16、`E2E_SMOKE_OK` 通過；外部連線 full verify 回傳 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。
- H-07 驗收完成：缺 key、非 object body、status mismatch、external failure、合法 `null`／空資料與 error-only fixture 均有明確分類；回退單位為單一 regression verifier commit，未修改產品 API、CSP、部署設定或 golden baseline。

分批方案：

- `H-07-01` endpoint／欄位政策盤點：逐一整理 44 個 endpoint 的 manifest name、request path、response 類型、使用頁面、穩定必要欄位與可為 `null`／空陣列的欄位；明確區分「必須存在」與「值必須非空」，並為不納入 allowlist 的 endpoint 記錄理由。產出 required-key matrix，不能只列少數代表端點。
- `H-07-02` allowlist schema 與 failure classification 設計：以 manifest endpoint name 為 key，支援明確的頂層／dot-path 欄位；缺 key 才報 schema failure，`null`、空陣列、時變值仍依既有規則處理。定義 fixture、快取 API、live API、非 2xx／外部失敗的分類邊界，以及 allowlist coverage 自身的完整性檢查。此批只提交設計與測試案例，不修改 verifier。
- `H-07-03` offline verifier 實作：先接到 loopback fixture／`--quick`，加入每個政策類型的 mutation tests（移除必要欄位必 FAIL、保留但為 `null`／空資料依政策 PASS、額外欄位不誤報、未登記 endpoint 不可靜默漏檢）；不更新 golden baseline，不接外部資料源。
- `H-07-04` live／full rollout 驗證：在 offline 全綠後才納入 `--api-live`／`--full`；核對 44 個 endpoint 的分類、21 頁 frontend、94 條 interaction、security 與 E2E，更新本計畫與 TD-19 matrix。若任何既有合法空資料被誤判，回退單一 verifier commit，不調低規則或修改 baseline 掩蓋差異。

每批驗收與護欄：

- 語法：`python -m py_compile` 所有受影響 Python；測試：既有 175 tests 加上 TD-19 專用 regression tests；安全：`python security_guardrail_check.py` 16/16；E2E：`E2E_SMOKE_OK`；最後依批次跑 `python regression/verify_against_baseline.py --full`，外部錯誤須標記 `[外部問題]`，不得混同為 schema failure。
- 不修改產品 route／response、CSP、部署設定或既有 baseline；只允許修改 regression verifier、專用測試與本計畫／矩陣文件。任何 validator 行為變更在 H-07-02 方案確認前不得實作。
- 完成條件：44/44 endpoint 都有明確 required／not-required 政策與理由；缺失必要欄位能穩定報出 endpoint、path 與 missing key；合法 `null`／空資料與外部失敗分類不退化；offline、live、full 三條驗證路徑均可重現。

H-07-01～H-07-04 執行前，先確認 H-07-02 的 schema、coverage 範圍與 failure classification；確認後一次只執行一批，完成驗證並回寫本計畫。

### 後續四項技術債規劃（2026-08-31，僅規劃）

前一版進度摘要將 TD-02 與 TD-18 的打包議題分開列示，造成「四項／五項」數量不一致。本節將 TD-02 的 classic-script 拆分視為已完成，將尚未完成的 bundling／minify／code splitting 統一歸入 TD-18；本階段規劃四個獨立後續項目：TD-03、TD-13、TD-15、TD-18。以下不代表已取得實作或部署授權。

| 建議順序 | 後續批次 | 對應 TD | 規劃目標 | 主要交付物 | 開始條件 |
|---:|---|---|---|---|---|
| 1 | H-08 | TD-13 | 收斂尚未治理的非 runtime／工具腳本 broad catch，維持既有 fallback 行為並補最低限度 context。 | 剩餘 catch inventory、逐批 logging contract、保留／排除理由與驗證紀錄。 | 先完成現況掃描；不做整檔格式化、不改 API 行為。 |
| 2 | H-09 | TD-15 | 重新核實 CSS dynamic／grouped candidates；只有三方證據一致才移除。 | 每組候選的 AST／字串／runtime 或頁面證據、刪除 diff、pixel／interaction 驗證結果。 | 不處理未列入候選的死碼；證據不一致即保留。 |
| 3 | H-10 | TD-18 | 以 deterministic shadow build 驗證 common runtime＋route bundle，再做單頁 canary。 | 固定版本與 hash 的 shadow artifact、`index.html`／`futures.html` canary 報告、classic-slice 回退路徑。 | 先確認 bundler、Node／lockfile、source map、CSP 與 cache invalidation 方案；不得覆寫現行 assets。 |
| 4 | H-11 | TD-03 | 在 staging 條件具備後完成 Redis 實連、剩餘 L2 bucket 與多 worker canary，最後才評估 rollout。 | Redis provider／成本決策、實連故障矩陣、雙 worker 壓測、7 個剩餘 bucket 清單、部署與回退紀錄。 | 需 `MARKET_PULSE_REDIS_URL`、可用 Redis staging、備份與 rollout 授權；未具備前維持單 worker。 |

#### H-08：TD-13 剩餘例外治理

- `H-08-01`：重新掃描 `app.py`、routes、fetchers、parsers、builders 與 regression／工具腳本，將每個剩餘 broad catch 分為 runtime fallback、工具腳本可接受例外、需補 context、需另立行為修復四類；不得以歷史清單直接視為現況。
- `H-08-02`：一次處理一個模組或明確的一組 catch；只補 logger context／contract test，保留既有回傳值、fallback 與錯誤分類。涉及 API 行為、例外升級或 fallback 改變時停止並另提方案。
- `H-08-03`：完成剩餘 inventory 對帳、六層驗證與 TD-13 狀態更新；不得因降低 catch 數量而刪除有意義的 fallback 或大規模重寫。

H-08-01 執行結果（2026-08-31，現況掃描）：

- 以 AST 重新掃描目前工作樹的 `except Exception`／`except BaseException`；H-08 指定範圍共 167 個：`app.py` 2、`routes_*.py` 35、`fetchers.py` 46、`parsers.py` 6、`builders.py` 61，以及 regression／工具腳本 17 個（`live_source_validation.py` 1、`portable_check.py` 5、`security_guardrail_check.py` 1、`regression/frontend_check.py` 1、`regression/interaction_check.py` 4、`regression/reproduce_td15_option_control.py` 1、`regression/td18_frontend_measure.py` 4）。
- 依 catch 的實際控制流程分類：production runtime 150 個屬既有 API error response、資料 fallback、retry／fan-out fallback 或已具 logging context 的 runtime fallback；regression／工具腳本 17 個屬可接受的工具例外（轉成結構化失敗結果、明確的 timeout／頁面重試退路，或測試／重現腳本記錄後重拋）。
- 本次盤點未發現需另立行為修復；工具 catch 原有結構化失敗結果或明確退路，但 `portable_check.py` health retry 未保留 attempt／endpoint 的診斷 log，因此列為 H-08-02 的低風險補 context 組。沒有為了降低 catch 數量刪除 fallback。
- 全 repo 另見 12 個不屬 H-08 的 broad catch：`cache.py` 7、`security.py` 3、`shared_state.py` 1、`test_shared_state.py` 1；前 11 個屬 TD-03／既有安全 fallback，測試 catch 屬測試輔助流程，均保留並記錄為排除範圍。
- 驗證：Python／Node 語法檢查通過；`python -m unittest test_derivatives_platform.py` 為 175 tests PASS；`security_guardrail_check.py` 為 16/16 PASS；`e2e_smoke.py` 為 `E2E_SMOKE_OK`。受限環境第一次 full verify 的 TWSE 502 與瀏覽器 `ERR_NETWORK_ACCESS_DENIED` 已標記為 `[外部問題]`，未視為程式碼回歸；在允許外部連線環境重跑同一命令後為 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。
- H-08-01 盤點本身未更新任何 API／frontend／interaction baseline、CSP、部署設定、SQLite 或 `twse-cache.json`；後續 H-08-02 另以單一工具 catch 組執行，H-09～H-11 不在本批執行。

H-08-02 執行結果（2026-08-31，`portable_check.py` health retry）：

- 在 `portable_check.py` health readiness retry 的 broad catch 補上 `LOGGER.debug(..., exc_info=exc)`，記錄 attempt 與 `/api/health` endpoint；保留原本 40 次輪詢、0.5 秒等待與 timeout failure 行為，不改工具的 pass／fail 分類。
- 新增 `test_portable_check_health_probe_logs_retry_context` mock contract test，驗證第一次 health probe 失敗仍會重試、讀取後續 stock search，並保留成功回傳與 process cleanup。
- `python -m py_compile portable_check.py test_derivatives_platform.py`、targeted contract test 與完整 `python -m unittest test_derivatives_platform.py`（176 tests）通過；`security_guardrail_check.py` 16/16、`e2e_smoke.py` `E2E_SMOKE_OK`。受限環境 full verify 的 TWSE 502／`ERR_NETWORK_ACCESS_DENIED` 依規範分類為 `[外部問題]`；允許外部連線重跑後 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。
- 本子批次未修改 API response、CSP、部署設定、任何 baseline、SQLite 或 `twse-cache.json`；H-08-02 只完成此一個工具 catch 組，下一個 H-08 子批次須另行授權。

H-08-03 執行結果（2026-08-31，inventory 對帳與 TD-13 狀態更新）：

- H-08-02 後以 AST 重新掃描，H-08 範圍仍為 167 個 broad catch：production runtime 150 個、regression／工具腳本 17 個；另有 `cache.py` 7、`security.py` 3、`shared_state.py` 1、`test_shared_state.py` 1 共 12 個排除項，分類與 H-08-01 一致，未出現遺漏或未分類 catch。`portable_check.py` health retry 已具備 attempt／endpoint logging 與 contract test。
- `git check-attr` 確認受影響 Python 檔維持 `text: auto`、`eol: lf`；未進行整檔換行改寫，未刪除既有 fallback，未修改 API／驗證行為。
- 六層驗證：`py_compile` 通過；完整 unit 176 tests PASS；security 16/16 PASS；`e2e_smoke.py` 為 `E2E_SMOKE_OK`；受限環境 full verify 的 TWSE 502／`ERR_NETWORK_ACCESS_DENIED` 分類為 `[外部問題]`，允許外部連線重跑後 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。
- H-08-03 完成後 TD-13 更新為「✅ 已完成／實質解決」；後續僅維護新增 broad catch 的 context／contract 規則。H-08 批次結案，H-09～H-11 仍未執行。未更新任何 baseline、CSP、部署設定、SQLite 或 `twse-cache.json`。

H-09-01 執行結果（2026-08-31，TD-15 dynamic candidates 重新核實）：

- 依完整掃描範圍重新核對 `docs/td15_css_classification.md` 的 81 個 dynamic rows：`is-*` 37、`ma-*` 6、`metric-*` 11、`market-ai-risk-*` 3、`market-risk-*` 3、`sector-card` 1 均有可定位的 JS／HTML 組合或直接引用證據；另 7 個 `asset-*` 與 13 個其餘 `sector-*` 只有前綴命中，尚無候選精確字串的產生器，依保守原則列為證據不足、保留。
- 21 頁 frontend manifest 與 21 頁／94 steps interaction manifest 已對帳；完整驗證仍以既有 baseline 比對，不以更新 baseline 消除差異。
- 驗證：本批無 Python／JS 變更；`git diff --check` 通過；`python -m unittest test_derivatives_platform.py` 為 176 tests PASS；`security_guardrail_check.py` 為 16/16 PASS；`e2e_smoke.py` 為 `E2E_SMOKE_OK`。受限環境 full verify 的 TWSE 502／瀏覽器 `ERR_NETWORK_ACCESS_DENIED` 已分類為 `[外部問題]`；允許外部連線重跑同一命令後為 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。
- 本批只更新分類文件與本計畫紀錄；未刪除任何 CSS，未修改 baseline、CSP、部署設定、SQLite 或 `twse-cache.json`。H-09-02／H-09-03 不在本批執行。

H-09-02 執行結果（2026-08-31，TD-15 grouped selectors 逐組核實）：

- 以目前 `split-01.css`～`split-07.css` 重新解析 216 個 class candidates，得到 247 個 candidate-bearing rule blocks；188 個含非候選 shared branch／compound class，59 個 candidate-only blocks。188 個 mixed blocks 涵蓋 107 個候選，16 個在 responsive `@media`、13 個含 active／focus／hover 狀態。
- 12 個全數降級 grouped candidates 已逐組記錄 shared selector、cascade、responsive 與狀態依賴；其餘 mixed blocks 依同一安全標準保留。candidate-only blocks 只列為後續三方證據候選，本批未刪除。
- 本批只更新 `docs/td15_css_classification.md` 與本計畫紀錄；未修改 CSS、baseline、CSP、部署設定、SQLite 或 `twse-cache.json`。H-09-03 不在本批執行。
- 驗證：本批無 Python／JS 變更；`git diff --check` 通過；176 tests PASS、security 16/16 PASS、`E2E_SMOKE_OK`；受限環境 full verify 的 TWSE 502／`ERR_NETWORK_ACCESS_DENIED` 分類為 `[外部問題]`，允許外連重跑後 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。

H-09-03 執行結果（2026-08-31，TD-15 單一候選組）：

- 依三方證據選定 `us-sector-end-label`：刪除前僅存在於 `split-03.css:2998,3002,3006` 的 3 個 compound state rule；完整 HTML／JS 無直接或樣板產生路徑，現行 render path 使用 `sector-end-label`，且無 shared live branch 或 responsive 依賴。
- 使用 `regression/delete_css_rules.py` 精確刪除上述 3 個 rule blocks；刪除後 `us-sector-end-label` 全 CSS 零命中，未改動其他候選或相鄰 rule。這是本批唯一刪除組，其他候選不順手處理。
- 驗證：`python -m py_compile app.py`、全 JS `node --check`、176 tests、security 16/16、`E2E_SMOKE_OK` 均通過；受限環境 full verify 的 TWSE 502／`ERR_NETWORK_ACCESS_DENIED` 分類為 `[外部問題]`，允許外連重跑後 `VERIFY_OK`，cached/live API、21 頁 frontend、94 條 interaction 與 security 全部通過。
- 本批已更新分類與計畫 MD；未更新 baseline、CSP、部署設定、SQLite 或 `twse-cache.json`。下一個候選組須另行授權。

#### H-09：TD-15 CSS 候選重新核實

- `H-09-01`：針對 `docs/td15_css_classification.md` 的 dynamic candidates，確認 selector 的靜態組合、HTML／JS 產生路徑與 21 頁實際載入／互動證據；先只產出分類，不刪除。
- `H-09-02`：針對 grouped selectors，逐組檢查 shared selector、media query、active／focus／responsive 狀態與 cascade 依賴；任何不確定項目保留並記錄理由。
- `H-09-03`：僅對三方證據一致的單一候選組執行刪除；完成 node syntax、21 頁 screenshot、94 條 interaction、security 與 full verify 後，才進下一組。不得更新 baseline 掩蓋未分類 pixel diff。

#### H-10：TD-18 bundling shadow／canary

- `H-10-01`：固定 Node／bundler 版本、lockfile、輸入清單、輸出命名、source map 與 artifact hash；連續兩次 shadow build 必須 deterministic，且產物置於隔離目錄。
- `H-10-02`：先驗證 common runtime＋route bundle 的 global scope、載入順序、`escapeHtml` 單一來源、CSP 與 local asset 404；現行 classic slices 與 HTML 入口保持可立即回退。
- `H-10-03`：以 `index.html` 為第一個 canary、`futures.html` 為第二個候選；需通過既有 H-06-04 的 pixel、DOM、互動、效能與錯誤率門檻，才可提出全頁 rollout 方案。
- `H-10-04`：bundler、HTML script 標籤、headers、`CACHE_VERSION`、Service Worker 或部署設定的實際變更，另立變更批次並先取得確認；本規劃不代表可直接上線。

H-10-01 執行結果（2026-08-31）：

- 建立 regression/td18_shadow_build.js 與 regression/td18_shadow_build.lock.json；lockfile 固定 Node 24.18.0、td18-shadow-concat 1.0.0、Node 內建 fs／crypto 實作、source map v3、輸入順序與輸出命名。
- 以現行 pwa.js、15 個 js/*.js classic slices、app.js 與 derivatives-ui.js 為固定輸入，產生 common-runtime.js、route-bundle.js 與 derivatives-status-addon.js，各自附固定 source map；產物均寫入隔離的 .tmp/td18-shadow-build/h10-01-20260831/run-1/ 與 run-2/，未覆寫現行 assets。
- 兩次 shadow build 的 3 個 bundle 與 3 個 source map bytes／SHA-256 完全一致；artifact hash 為：common-runtime.js 460901 bytes / 124e746d1967f6af9d5058d43ed982e6e99013f52d889ee974f0a7819afeaa59、route-bundle.js 1234069 bytes / 512d98d4148ef5c067e027e0420a30d5fc707c20f25288aa76c718d6255c20ed、derivatives-status-addon.js 4330 bytes / 698ba729c1fbd30e230526dacab26c0e8e55b0e007e3235e6f911e07b9225d94；對應 source map hash 分別為 08ecee22b905507278728a2f0a04429073788e60886207a538b4ec5b85d156a0、7f6672728a562d44106fb6e3b4eb1fcc0829d5431a7a7bfe72bace206d2bf038、6a34b6fc11907960b4883d478aa20fa353155147002e4a76a4f79a8eb81001cd。
- 驗證：shadow builder 與 3 個輸出 bundle 均 node --check PASS；176 tests PASS；security 16/16 PASS；E2E_SMOKE_OK；受限環境 full verify 的 TWSE 502／ERR_NETWORK_ACCESS_DENIED 依規範分類為外部問題，允許外連重跑後 VERIFY_OK，cached／live API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- 本批僅新增 shadow builder、lockfile 與隔離產物；未修改 production assets、HTML、baseline、CSP、部署設定、SQLite 或 twse-cache.json。H-10-02 的 global scope／載入順序／CSP／local asset 404 驗證，以及任何正式 bundler／HTML／部署接線，須另批授權。

H-10-02 執行結果（2026-08-31）：

- 建立 regression/td18_shadow_verify.py；每次先依 H-10-01 lockfile 在隔離暫存目錄重建 shadow artifacts，再驗證 metadata、source map、bundle syntax 與現行 classic-slice fallback，不讀寫正式 SQLite 或 twse-cache.json。
- Global scope：common-runtime＋route-bundle 解析結果為 895 個 baseline symbols／895 個 shadow symbols，集合完全一致；escapeHtml 定義數為 1，且位於 common-runtime。
- Load order／fallback：21 頁 HTML 的 script order 均與 lockfile 一致；derivatives-status.html 的 derivatives-ui.js addon 亦符合固定尾端順序；index.html 與 futures.html 原入口 HTTP 200，現行 classic slices 未被替換。
- CSP／local assets：實際本機 server response 核對為 script-src 'self'、frame-ancestors 'none'，script-src 無 unsafe-inline／unsafe-eval；所有 21 頁本地 JS／CSS 資產 HTTP 404 數為 0。
- 驗證：專用 verifier 輸出 H10_02_VERIFY_OK；shadow verifier／builder 與輸出 bundle node syntax PASS；176 tests PASS；security 16/16 PASS；E2E_SMOKE_OK；受限環境 full verify 的 TWSE 502／ERR_NETWORK_ACCESS_DENIED 依規範分類為外部問題，允許外連重跑後 VERIFY_OK，cached／live API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- 本批僅新增 H-10-02 verifier 與計畫紀錄；未接線 HTML／shadow bundle、未修改現行 assets、baseline、CSP、部署設定、SQLite 或 twse-cache.json。H-10-03 canary 與任何正式 bundler／HTML／部署接線仍須另批授權。

H-10-03 執行結果（2026-08-31，TD-18 index／futures shadow canary）：

- 建立 `regression/td18_canary.py`；在 `.tmp/td18_h10_03_*` 隔離目錄建立 classic 與 shadow 臨時頁面，沿用既有 frontend／interaction HAR，shadow 僅替換臨時副本的 script tags，並使用 production-like CSP；`index.html` 先驗證，只有全數通過後才進入 `futures.html`。
- `index.html` 通過：pixel diff `0.000%`、DOM counts `47 cards／0 table rows` 與 baseline 一致、`index__penny-trend-market` 互動 `1/1`；classic→shadow asset requests `25→10`（減少 15），transfer bytes `470,912→458,856`；cold p50 meaningful render `2368.2→2367.9ms`，warm p50 `2296.5→2237.7ms`。
- `futures.html` 通過：pixel diff `0.084%`、DOM counts `22 cards／58 table rows` 與 baseline 一致、互動 `11/11`；classic→shadow asset requests `25→10`（減少 15），transfer bytes `470,912→458,856`；cold p50 meaningful render `3169.3→3406.1ms`（回歸約 7.5%，低於 10% 門檻），warm p50 `3144.3→3148.3ms`；兩頁 local request／HTTP failure、page error 與 console error 均為 0，外部資源錯誤為 0。
- 驗證：canary `H10_03_CANARY_OK`；`py_compile`、`node --check`、176 tests、security 16/16、`E2E_SMOKE_OK` 均通過；受限 full verify 的 TWSE 502／`ERR_NETWORK_ACCESS_DENIED` 分類為外部問題，允許外連重跑同一命令後 `VERIFY_OK`，cached/live API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- 本批僅新增隔離 canary verifier 與計畫紀錄；未修改正式 HTML／assets、未接線 shadow bundle、未更新 baseline、CSP、headers、CACHE_VERSION、Service Worker、部署設定、SQLite 或 twse-cache.json。classic slices 保持立即回退；任何正式 rollout／HTML script tag／部署變更須另批授權。

H-10-04 執行結果（2026-08-31，TD-18 production bundle wiring）：

- 依 H-10-01 lockfile 產出並供應 `common-runtime.js`、`route-bundle.js`、`derivatives-status-addon.js` 與 3 個 v3 source maps；`td18-bundle-manifest.json` 固定 Node 24.18.0／builder 1.0.0／input order／artifact hash。三個 bundle hash 與 H-10-01 deterministic artifact 一致：`124e746d...9afeaa59`、`512d98d4...5c20ed`、`698ba729...9225d94`。
- 21 個 HTML 入口已改為版本化 query 的 `common-runtime.js` → `route-bundle.js`；`derivatives-status.html` 另接 `derivatives-status-addon.js`。原有 classic `js/*.js`、`app.js` 與 `derivatives-ui.js` 原始檔仍保留，可回退至 classic wiring；Flask root static whitelist 明確列入 bundle／source map，未開放任意檔案。
- `regression/td18_shadow_verify.py` 驗證 production-bundle mode：21/21 pages、895/895 global symbols、escapeHtml 1 份、CSP `script-src 'self'`／`frame-ancestors 'none'`、local asset 404=0；bundle manifest hash check 與所有 bundle `node --check` 通過。CSP／headers 維持原值，未修改部署入口（`app:app`）或 deployment config。
- 六層驗證：176 tests PASS；security 16/16 PASS；`E2E_SMOKE_OK`；受限 full verify 的 TWSE 502／`ERR_NETWORK_ACCESS_DENIED` 分類為外部問題，允許外連重跑同一命令後 `VERIFY_OK`，cached/live API、21/21 frontend compare、94/94 interaction compare 與 security 全部通過。
- 本批未修改 baseline、CSP、headers、`CACHE_VERSION`、Service Worker 或部署設定；此次 bundle 與既有 backend API 向後相容，且以新 query 版本避免沿用舊 bundle cache，故不需清除既有 Service Worker cache。未修改 SQLite、twse-cache.json 或其他正式資料；H-10 批次完成，後續若需 CSS 合併、cache policy 或部署策略變更須另立批次。

H-10-05 執行結果（2026-09-01，TD-18 formal minify／cache／HTML rollout）：

- 建立 `package.json`／`package-lock.json` 固定 Node `24.18.0`、Terser `5.51.2` 與 `regression/td18_minify_build.js`；3 個 minified bundle 均附 Terser v3 source map。common runtime 減少 `37.37%`、route bundle 減少 `27.64%`、status addon 減少 `14.19%`。
- `regression/td18_minify_verify.py` 通過：兩次 build deterministic、895 global tokens 無缺漏、`escapeHtml` 定義 1 份、所有 minified bundle `node --check` 通過；`td18-minify-bundle-manifest.json` 記錄正式 hash／cache policy／rollback。
- 21 頁 HTML 已切換至 `common-runtime.min.js` → `route-bundle.min.js`，status 頁追加 minified addon；Flask whitelist 已加入 minified assets／maps，帶正確版本 query 時回應 `public, max-age=31536000, immutable`，未帶版本 query 仍維持 no-store。
- `service-worker.js`／`pwa.js` 已同步 bump 至 `td18-minify-20260901-1`，啟用新 cache namespace 並清理舊 runtime cache；CSP 仍為 `script-src 'self'`／`frame-ancestors 'none'`，未加入 `unsafe-inline`／`unsafe-eval`。`Procfile`／`render.yaml` 維持 `app:app` 與單 worker。
- minified index／futures canary 通過：pixel diff `0.000%`／`0.084%`、DOM／互動 `1/1`／`11/11`、asset requests `25→10`、gzip transfer 約 `470,910→389,314` bytes；完整驗證為 176 tests、security 16/16、`E2E_SMOKE_OK`、`VERIFY_OK`（21/21 frontend、94/94 interaction）。
- 本批未更新 baseline、SQLite、`twse-cache.json` 或 CSP 放寬內容；classic slices 與未壓縮 bundle 保留，可切回舊 query／入口作為 rollback。CSS 合併、preload 與其他部署策略仍未導入。

H-11-01 執行結果（2026-08-31，Redis staging provider／成本／region／secret 決策與 preflight）：

- 完成 `docs/TD03_H11-01_redis_staging_decision_2026-08-31.md`；首選與 Render web service 同 workspace／同 region 的 Render Key Value，Upstash 僅作 Render 不可用時的隔離 benchmark 備選。成本以最小 staging plan、dashboard 當期報價與明確月費上限為 gate，不在 repo 寫死或本批建立付費資源；region 因 `render.yaml` 未宣告而保留為 provisioning 前必填。
- 完成 secret、namespace、provider snapshot／RDB、故障前 checkpoint／隔離 restore、PING／短 TTL sentinel／Redis contract 的 runbook 與驗收門檻；既有 shared client timeout 預設 1 秒、rate-limit fail-closed、L2／single-flight／background lease 降級策略已對齊記錄。
- preflight 確認 `MARKET_PULSE_REDIS_URL` 與 `REDIS_URL` 均 absent，故本批未做實連、未做 backup／restore，也未宣稱 Redis staging ready；H-11-02 的 7 個 L2 bucket 與 H-11-03 雙 worker canary 不在本批。
- 驗證：offline canary `TD03_SHARED_STATE_CANARY_OFFLINE_OK`；`redis==8.1.0` import、Python syntax、文件 diff check 通過；shared-state／cache／TD-03 canary／derivatives unit 共 200 tests PASS、1 skip（無 Redis URL）；security 16/16 PASS；`E2E_SMOKE_OK`；`verify_against_baseline.py --full` 已執行，受限環境外部資料／瀏覽器阻擋依規範標為 `[外部問題]`，未更新 baseline。
- 本批未修改 baseline、CSP、headers、`CACHE_VERSION`、Service Worker、`Procfile`、`render.yaml`、worker 數、SQLite 或 `twse-cache.json`；所有 shared modes 維持預設關閉／local。

H-11-02 執行結果（2026-08-31，7 個 L2 bucket JSON boundary／TTL／L1 promotion／failure contract）：

- 6 個 JSON-safe bucket 已加入 `cache.py` `CACHE_L2_BUCKETS`，並將直接操作 process-local dict 的路徑改為共用 accessor：`global_markets`、`sector_charts`、`stock_details`、`taifex_options_chain`、`us_etf_center`、`yahoo_tw_option_chain`。`stock_details` 補入中央 `stock_detail` 5 分鐘 TTL；其他 bucket 沿用既有 registry TTL。
- 第 7 個候選 `external_text` 維持排除，因同一 bucket 同時承載文字與 binary bytes，無法安全通過 Redis JSON boundary；測試明確確認 Redis mode 下不會呼叫 L2 adapter，保留原本 binary local cache 行為。
- 新增逐 bucket contract：JSON round-trip、TTL expiry、L1 miss promotion、disconnect fallback；offline canary `TD03_SHARED_STATE_CANARY_OFFLINE_OK`；shared-state／cache／TD-03 canary／derivatives unit 共 202 tests PASS、1 skip（`MARKET_PULSE_REDIS_URL` absent）。
- 驗證：受影響 Python syntax、security 16/16、`E2E_SMOKE_OK` 與 `verify_against_baseline.py --full` 均已執行；受限環境的外部資料／瀏覽器錯誤依規範為 `[外部問題]`，未更新 baseline。`MARKET_PULSE_REDIS_URL` 未設定，故未宣稱 Redis 實連或 staging ready。
- 本批未修改 baseline、CSP、headers、`CACHE_VERSION`、Service Worker、`Procfile`、`render.yaml`、worker 數、SQLite 或 `twse-cache.json`；H-11-03 雙 worker staging 壓測與 H-11-04 rollout 仍待後續授權。

H-11-03 執行結果（2026-08-31，雙 worker load／fault-injection canary）：

- 新增隔離 `regression/td03_h11_03_canary.py` 與 `test_td03_h11_03_canary.py`；只使用 in-memory shared adapter 與隔離 Flask request context，不 import production `app.py`、不開 Redis connection、不讀寫正式資料。
- 雙 worker concurrent load 共 100 requests（每 worker 50），共享 rate-limit window 為 50 accepted／50 rejected；6 個 H-11-02 L2 bucket 均完成 cross-worker JSON readback。`cache-flight`、`options-flight`、`background-updater` 均通過 single-owner、takeover；options lease 另通過 expired takeover 與 owner-token fencing。
- fault injection 結果：rate-limit `503 RATE_LIMIT_BACKEND_UNAVAILABLE` fail-closed；cache L2 與 generic／options single-flight local fallback；background updater skip 該輪，不建立多 worker refresh。
- `MARKET_PULSE_REDIS_URL` 仍 absent，real Redis canary 為 `not-run`；因此本批完成 offline 雙 worker contract，但未宣稱實 staging load／latency／disconnect 壓測完成，H-11-04 rollout 不在本批。
- 驗證：`TD03_H11_03_CANARY_OFFLINE_OK`；205 tests PASS、1 skip（無 Redis URL）；security 16/16、`E2E_SMOKE_OK` 與 `verify_against_baseline.py --full` 均已執行，外部網路錯誤依規範為 `[外部問題]`。未更新 baseline。
- 本批未修改 CSP、headers、`CACHE_VERSION`、Service Worker、`Procfile`、`render.yaml`、worker 數、SQLite 或 `twse-cache.json`。

H-11-04 執行結果（2026-08-31，rollout eligibility gate／rollback readiness）：

- 新增唯讀 `regression/td03_h11_04_rollout_gate.py` 與 `test_td03_h11_04_rollout_gate.py`；輸出 `TD03_H11_04_ROLLOUT_GATE_BLOCKED`、`eligible=false`。safe state 通過：四個 shared modes 均為預設 `local`，`Procfile`／`render.yaml` 均為 `--workers 1`，入口仍為 `app:app`。
- rollout gate 未通過項目：`MARKET_PULSE_REDIS_URL`、Redis PING／sentinel、backup／restore、H-11-02 real Redis contract、H-11-03 real dual-worker staging load／latency／disconnect evidence、觀測期與人工 rollout approval。offline evidence 不替代實 staging evidence。
- 已建立 [TD03 H-11-04 rollout gate 與 rollback runbook](docs/TD03_H11-04_rollout_gate_2026-08-31.md)；未 provisioning、未切換 shared modes、未增加 worker。未來僅在全部 gate 與人工核准完成後另立 deployment batch；rollback 順序為 shared modes local → 單 worker → adapter／deployment rollback。
- 驗證：gate tests 2/2 PASS；shared-state／cache／canary／derivatives unit 共 207 tests PASS、1 skip（無 Redis URL）；security 16/16、`E2E_SMOKE_OK` 與 `verify_against_baseline.py --full` 均已執行，外部網路錯誤依規範為 `[外部問題]`，未更新 baseline。
- 本批未修改 baseline、CSP、headers、`CACHE_VERSION`、Service Worker、`Procfile`、`render.yaml`、worker 數、Redis URL、SQLite 或 `twse-cache.json`。

H-11 Redis 實連與多 worker staging rollout 封存（2026-08-31）：

- 依授權暫不處理尚未完成的 Redis 實連與多 worker staging rollout，另立 [封存紀錄](docs/TD03_H11_redis_rollout_deferred_2026-08-31.md)。
- 目前 `MARKET_PULSE_REDIS_URL` 未注入；Redis health、backup／restore、real bucket contract、real dual-worker staging evidence、觀測期與人工 rollout approval 均未完成，rollout eligibility 維持 `eligible=false`。
- 封存期間維持 shared modes `local` 與 `--workers 1`；不 provisioning、不切換 shared modes、不增加 worker、不發佈。解封須重新授權，並先具備隔離 staging URL、完整 staging evidence、觀測期與人工核准。

#### H-11：TD-03 Redis 實連與多 worker rollout

- `H-11-01`：先完成 provider／成本／延遲／資料區域與 secret 管理決策，建立 staging backup、health check、timeout 與 fail-closed／降級策略；確認 `MARKET_PULSE_REDIS_URL` 可用後才執行實連測試。
- `H-11-02`：依風險分組導入尚未納入 L2 的 7 個 bucket：`external_text`、`global_markets`、`sector_charts`、`stock_details`、`taifex_options_chain`、`us_etf_center`、`yahoo_tw_option_chain`；每組驗證 JSON 邊界、TTL、L1 回填、miss、timeout、disconnect 與 stale data 行為。
- `H-11-03`：執行雙 worker staging 壓測與 fault injection，核對 rate-limit、single-flight、options lease、background updater 的 single-owner、fencing、takeover 與 fail-closed 契約；未通過即關閉 shared mode 並維持 `--workers 1`。
- `H-11-04`：只有 staging canary、完整驗證、觀測週期與人工 rollout 核准均通過，才另立部署變更批次；回退順序為關閉 shared mode → 恢復單 worker → 回退單一 adapter／deployment commit，不修改 SQLite 或 `twse-cache.json`。

四項共同驗收：每個子批次均須完成受影響檔案語法檢查、unit tests、security 16/16、`E2E_SMOKE_OK`、`verify_against_baseline.py --full`，並依 `[外部問題]`／`[程式碼可能改動回應格式]` 分類；不得未經確認更新既有 baseline、CSP、部署設定或正式資料。

### TD-02／TD-15／TD-18 後續規劃索引（2026-08-31；TD02-01～03、TD15-01～03 已完成既有批次）

已將 TD-02、TD-15、TD-18 的後續範圍、子批次、依賴、驗收 gate 與回退點整理至
[TD-02／TD-15／TD-18 後續改善規劃](docs/TD02_TD15_TD18_followup_plan_2026-08-31.md)。
TD02-01、TD02-02、TD02-03 與 TD15-01～TD15-03 已依授權完成盤點、設計、shadow proof 與既有 CSS 批次；正式 ESM／HTML wiring、TD-15 residual candidates 及其他後續批次仍須依證據逐批處理並回寫紀錄。

TD-15 residual candidates 的後續處理已另立 [逐批規劃](docs/TD15_residual_candidates_plan_2026-08-31.md)：先做 R0 現況重建／歷史對帳，再分流 candidate-only CSS、grouped／responsive／state 再評估與 JS residual re-audit；本階段不刪除、不更新 baseline。
R0 已完成：新增 [td15_residual_inventory.py](regression/td15_residual_inventory.py)，產出 [R0 JSON manifest](docs/TD15_residual_inventory_2026-08-31.json) 與 [R0 Markdown report](docs/TD15_residual_inventory_2026-08-31.md)；現況為 191 個含候選 class 的 CSS rule blocks、90 個候選名稱，歷史刪除對帳為 32 個 blocks。本次只做盤點，未刪除 CSS／JS 或更新 baseline。
R1-01 已完成：依現況 manifest 與三方證據刪除未載入 `assets/styles.css` 的 11 個 candidate-only rule blocks；刪除後 manifest 為 271 個含候選 class 的 blocks、0 個 candidate-only。完整驗證 `VERIFY_OK`（21/21 frontend、94/94 interaction），未更新 baseline；詳見 [R1-01 evidence](docs/TD15_R1_01_evidence_2026-08-31.md)。
R2-01 已完成：依 post-R1 manifest 與三方證據刪除未載入 `assets/styles.css` 的 8 個 grouped blocks；production split CSS 的 65 個 grouped／responsive／state blocks 全部保留。刪除後 manifest 為 263 個含候選 class 的 blocks、0 個 candidate-only。完整驗證 `VERIFY_OK`（21/21 frontend、94/94 interaction），未更新 baseline；詳見 [R2-01 evidence](docs/TD15_R2_01_evidence_2026-08-31.md)。
R3 已完成：新增 [td15_js_residual_audit.py](regression/td15_js_residual_audit.py) 與 [R3 audit](docs/TD15_R3_js_residual_audit_2026-08-31.md)；現況 `js/legacy-unclassified.js` 為空，歷史 58 個符號 literal／candidate dynamic hits 均為 0，H-04-11～H-04-15 不重複處理。global-symbol matrix 維持 895 symbols／69 edges，完整驗證 `VERIFY_OK`（21/21 frontend、94/94 interaction），未更新 baseline。

#### TD02-01 執行結果（2026-08-31）

- 新增 [TD02-01 Markdown 依賴矩陣](docs/TD02-01_dependency_matrix_2026-08-31.md)、[JSON 矩陣](docs/TD02-01_dependency_matrix_2026-08-31.json) 與可重現產生／比對工具 `regression/td02_01_dependency_matrix.py`。
- 現況證據核對 21 頁、895/895 global symbols、18 個 classic fallback inputs、69 條跨 slice edges、468 個 cross-slice references；矩陣逐項保留 owner、consumer、初始化引用、dynamic window bridge 與回退順序。
- 發現並封存 2 組需 TD02-02 處理的循環：`main ↔ page-tw`、`page-global-market-assethub ↔ page-global-market-futures ↔ page-global-market-options`。本批不直接導入 ESM、不改 HTML 行為。
- 本批未修改 baseline、CSP、headers、Service Worker、cache、部署設定、SQLite 或 `twse-cache.json`；classic fallback 與 H-10 production bundle 入口均保留。
- 驗證：`py_compile`、矩陣 `--check`、176 tests、security 16/16、根目錄 `e2e_smoke.py` 的 `E2E_SMOKE_OK` 通過；受限環境首次 full verify 的 TWSE 502／瀏覽器 `ERR_NETWORK_ACCESS_DENIED` 分類為 `[外部問題]`，允許外部連線重跑同一命令後 `VERIFY_OK`，包含 21/21 frontend 與 94/94 interaction，未更新 baseline。

#### TD02-02 執行結果（2026-08-31）

- 新增 [TD02-02 模組邊界／載入／回退／verifier 設計](docs/TD02-02_module_boundary_design_2026-08-31.md)。
- 定義 `runtime/*`、`route/*`、`compat/*` candidate naming；將 `main ↔ page-tw` 的循環交由 `runtime/live-data` contract 解開，global-market futures/options/assethub 保留為第一階段 module island。
- 定義 legacy bridge 四類契約與 fail-closed verifier：895/895 owner、import／bridge allowlist、SCC、top-level initialization、escapeHtml、CSP、dynamic window、DOM/pixel/interaction、classic rollback 與錯誤注入。
- 本批不建立正式 ESM、不改 HTML／runtime／baseline／CSP／部署；TD02-03 shadow-only proof 另批執行且未接線。
- 驗證：TD02-01 matrix `--check`、py_compile、176 tests、security 16/16、`E2E_SMOKE_OK` 通過；允許外部連線重跑 `python regression/verify_against_baseline.py --full` 輸出 `VERIFY_OK`，包含 21/21 frontend 與 94/94 interaction。未修改 baseline。

#### TD02-03 執行結果（2026-08-31）

- 新增 [TD02-03 `runtime/api` shadow proof](docs/TD02-03_api_shadow_proof_2026-08-31.md) 與可重跑 `regression/td02_03_api_shadow.py`；只在系統 temp 目錄生成一次性 ESM wrapper／Node harness，未改 `js/api.js`、HTML、bundle 或正式 asset。
- `js/api.js` 唯一 symbol 為 `fetchWithTimeout`；matrix 無 dependency edge／cycle，top-level immediate refs 為 0，DOM／window refs 為 0，classic order 為 `core → api → shared-calc`；全站 `escapeHtml` definition 維持 1 份且 owner 為 `js/core.js`。
- Runtime proof 通過 named export、import 無 fetch side effect、success／options／AbortController signal、upstream rejection 與 timeout `AbortError` 行為。
- 驗證：py_compile、shadow verifier、matrix `--check`、176 tests、security 16/16、`E2E_SMOKE_OK` 與允許外連 `VERIFY_OK`（21/21 frontend、94/94 interaction）全通過；未更新 baseline。正式 ESM／HTML wiring 仍須另批授權。

## 4. 明確暫不處理

- 不直接重寫整個 `app.js`，不一次導入 ES Modules/bundler；TD-18 先做評估。
- 不在沒有容量、備份、migration、故障模式與回退方案前切換 Redis/SQLite 共享狀態。
- 不因 TWSE、TAIFEX、Yahoo 或瀏覽器外部資源阻擋而更新 screenshot、API 或 interaction baseline。
- 不順手清理 TD-15 第二輪以外的死碼、CSS 候選或部署設定。
- 不修改正式 SQLite、`twse-cache.json`、HAR、ZIP、bundle 等執行期/建置產物進入版控。
- 不把原始稽核快照的歷史行號直接當作現況證據；現況以本文件矩陣、批次紀錄與可重現驗證結果為準。

## 5. 每個 H 批次的硬性驗證

每次只處理一個 H 批次或其明確子批次，完成後依序執行：

1. `python -m py_compile <changed .py>`
2. `node --check <changed .js>`
3. `python -m unittest test_derivatives_platform.py`
4. `python security_guardrail_check.py`
5. `python e2e_smoke.py`
6. `python regression/verify_against_baseline.py --full`

另依範圍追加：

- 前端：21 頁 frontend compare、94 條 interaction compare、global symbol scan。
- API/外部資料：offline fixture、`portable_check.py`，必要時再以允許外部連線的環境執行 live check。
- 交付：existing/clean package `verify_release_integrity.py`、cache/DB/pycache hash 與 bundle verification。

## 6. 完成定義

新的改善計畫只有在以下條件同時滿足時才可結案：

- 每一個 H 子批次有明確範圍、測試、驗證輸出與回退點。
- API schema、既有頁面行為、安全護欄與 interaction baseline 沒有未分類回歸。
- 任何外部失敗都標明資料源/環境阻擋，不與程式碼失敗混淆。
- 部分完成與明確延後項目仍保留可追蹤的進入點，不以「全部完成」文字掩蓋未做工作。

## 7. 執行紀錄

| 日期 | 批次 | 狀態 | 備註 |
|---|---|---|---|
| 2026-08-31 | TD02-03 | ✅完成（`runtime/api` shadow-only proof） | `fetchWithTimeout` named export、classic order、無 cycle／top-level side effect、escapeHtml single source、success／upstream error／timeout 行為全通過；只建立 temp wrapper／harness，未接線正式 ESM／HTML。 |
| 2026-08-31 | TD02-02 | ✅完成（模組邊界／載入契約／verifier 設計） | 產出 candidate module、legacy bridge、module island、live-data contract、fail-closed verifier 與 classic rollback 設計；未建立 ESM／未接線 HTML。 |
| 2026-08-31 | TD02-01 | ✅完成（依賴矩陣／候選模組邊界盤點） | 產出可重現 JSON／Markdown 矩陣與 `regression/td02_01_dependency_matrix.py`；核對 21 頁、895/895 symbols、69 edges、2 組循環與 dynamic window bridge。176 tests、security 16/16、E2E、允許外連 full verify（21/21 frontend、94/94 interaction）全綠；未接線 ESM、未改 HTML／baseline／CSP／部署。 |
| 2026-08-31 | H-11-ROLL-ARCHIVE | ⏸️暫不處理（Redis 實連／多 worker staging rollout 封存） | `MARKET_PULSE_REDIS_URL` absent；real Redis、backup／restore、實 staging evidence、觀測期與人工 rollout approval 未完成。已另立封存 MD；維持 shared modes `local`、單 worker，不修改 baseline／CSP／部署設定／正式資料。 |
| 2026-08-31 | H-11-04 | 🟡完成（rollout gate blocked；未建立部署變更） | safe state／1 worker gate 通過；Redis 實連、backup／restore、實 staging evidence、觀測期與人工 rollout approval 未滿足。gate tests 2/2、207 tests、security、E2E、full verify 已執行；未修改 baseline／CSP／部署設定／正式資料。 |
| 2026-08-31 | H-11-03 | 🟡完成（offline 雙 worker load／fault canary；實 staging 待 URL） | 100 requests、rate-limit 50/50、cache／options／background lease single-owner／takeover／fencing 與 fault policy 通過；205 tests、security、E2E、full verify 已執行。`MARKET_PULSE_REDIS_URL` absent，未實連／壓測／rollout，未修改 baseline／CSP／部署設定／正式資料。 |
| 2026-08-31 | H-11-02 | 🟡完成（6 bucket L2 導入；`external_text` 保守排除；實連待 staging） | 6 個 JSON-safe bucket 完成 accessor 接線、5 分鐘 stock detail TTL 與逐 bucket JSON／TTL／L1 promotion／disconnect contract；202 tests、security、E2E 與 full verify 已執行。`MARKET_PULSE_REDIS_URL` absent，未宣稱實連；未修改 baseline／CSP／部署設定／正式資料。 |
| 2026-08-31 | H-11-01 | 🟡完成（provider／成本／region／secret 決策與 offline preflight；實連待 staging） | 首選 Render Key Value，同 region／namespace／secret／backup／health／timeout／fail-closed／降級 gate 已記錄；`MARKET_PULSE_REDIS_URL` absent，未實連、未 provisioning、未修改 baseline／CSP／部署設定／正式資料。 |
| 2026-08-31 | H-10-04 | ✅完成（production bundle wiring） | 21 頁 HTML 接線至 common／route bundle，status addon 保持尾端順序；manifest hash、21/21 global／CSP／local assets、176 tests、security 16/16、E2E、21/21 frontend、94/94 interaction 與外連 full verify 全綠。classic sources 保留回退；未修改 baseline／CSP／headers／CACHE_VERSION／Service Worker／部署設定／正式資料。 |
| 2026-09-01 | H-10-05 | ✅完成（formal minify／immutable cache／SW／HTML rollout） | 固定 Terser 5.51.2；common／route／status minified bundle 減少 37.37%／27.64%／14.19%，895 global tokens、source map、minified canary、immutable cache header 與 SW cache version 全通過；21/21 frontend、94/94 interaction、176 tests、security、E2E 與 full verify 全綠。CSP 維持原值，`app:app`／單 worker 未變；classic／未壓縮 bundle 保留回退。 |
| 2026-08-31 | H-10-03 | ✅完成（index→futures shadow canary） | 依序完成兩頁隔離 canary：pixel diff 0.000%／0.084%、DOM counts 一致、互動 1/1／11/11、cold/warm p50 皆在 10% 效能門檻內；每頁 asset requests 25→10、transfer bytes 470,912→458,856；canary、176 tests、security 16/16、E2E 與外連 full verify 全綠。未接線 HTML／bundle，未修改 baseline／CSP／部署設定／正式資料。 |
| 2026-08-31 | H-10-02 | ✅完成（shadow scope／CSP／local asset verification） | 專用 verifier 核對 895/895 global symbols、escapeHtml 1 份、21 頁 script order、原入口 fallback、CSP 與 local asset 404=0；176 tests、security 16/16、E2E 與外連 full verify 全綠。未接線 HTML／bundle，未修改 baseline／CSP／部署設定／正式資料。 |
| 2026-08-31 | H-10-01 | ✅完成（deterministic shadow build） | 固定 Node 24.18.0、td18-shadow-concat 1.0.0、lockfile、輸入／輸出與 source map；兩次隔離 shadow build 的 3 bundles／3 maps bytes 與 SHA-256 完全一致，輸出均通過 node syntax；176 tests、security 16/16、E2E 與外連 full verify 全綠。未接線 HTML／現行 assets、未修改 baseline／CSP／部署設定／正式資料。 |
| 2026-08-30 | 現況覆核 | ✅完成 | 已核對稽核快照、A～G 紀錄、程式碼與測試；產出本 V2 計畫。 |
| 2026-08-30 | H-01 | ✅完成 | 選定 `renderTechnicalChart`；函式內拆出 overlay、hover zone、panel config、markup helper。六層驗證全綠，未更新 baseline。 |
| 2026-08-30 | H-02 | ✅完成 | TD-10 top-10 fixture contract matrix 完成；10 個大型 builder/fetcher 有固定輸出欄位與可重現測試，142 tests、full verify 全綠。 |
| 2026-08-30 | H-03-01 | ✅完成（第一子批次） | 完成 `builders.py` `build_stock_detail` 三個歷史資料 fallback broad catch 的 debug context 與 logging contract；六層驗證全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-02 | ✅完成（第二子批次） | 完成 `fetchers.py` Nasdaq company supplement 五個 fallback broad catch 的 debug context 與 logging contract；144 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-03 | ✅完成（第三子批次） | 完成 `parsers.py` `refresh_tpex_cache` 三個 fallback broad catch 的 debug context 與 logging contract；145 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-04 | ✅完成（第四子批次） | 完成 `fetchers.py` 台股歷史/法人資料五個 fallback broad catch 的 debug context 與 logging contract；148 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-05 | ✅完成（第五子批次） | 完成 `builders.py` sector-history 五個 fallback broad catch 的 debug context 與 logging contract；151 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-06 | ✅完成（第六子批次） | 完成 `fetchers.py` 國際市場指數/spot snapshot 三個 fallback broad catch 的 debug context 與 logging contract；153 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-07 | ✅完成（第七子批次） | 完成 `builders.py` Yahoo class quote/group 三個 fallback broad catch 的 debug context 與 logging contract；155 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-08 | ✅完成（第八子批次） | 完成 `fetchers.py` Yahoo class quote pagination 一個 fallback broad catch 的 debug context 與 logging contract；156 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-09 | ✅完成（第九子批次） | 完成 `fetchers.py` Nasdaq quote endpoint 一個 retry fallback broad catch 的 debug context 與 logging contract；157 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-10 | ✅完成（第十子批次） | 完成 `fetchers.py` U.S. Treasury yield curve 一個 fallback broad catch 的 debug context 與 logging contract；158 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-11 | ✅完成（第十一子批次） | 完成 `builders.py` `build_institution_trend` 一個 fallback broad catch 的 debug context 與 logging contract；159 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-12 | ✅完成（第十二子批次） | 完成 `fetchers.py` `fetch_market_macro_factors` 一個 per-factor fallback broad catch 的 debug context 與 logging contract；160 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-13 | ✅完成（第十三子批次） | 完成 `fetchers.py` `collect_futures_until_deadline` 一個 future-exception fallback broad catch 的 debug context 與 logging contract；161 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-14 | ✅完成（第十四子批次） | 完成 `builders.py` `estimate_us_beta` 一個 benchmark fallback broad catch 的 debug context 與 logging contract；162 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-15 | ✅完成（第十五子批次） | 完成 `builders.py` `build_us_market_symbol_detail` 一個長期歷史 fallback broad catch 的 debug context 與 logging contract；163 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-16 | ✅完成（第十六子批次） | 完成 `builders.py` `build_us_market_symbol_detail` 三路 component fan-out 一個 fallback broad catch 的 debug context 與 logging contract；164 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-17 | ✅完成（第十七子批次） | 完成 `builders.py` `build_site_data` 三個 source bootstrap fallback broad catch 的 debug context 與 logging contract；165 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-18 | ✅完成（第十八子批次） | 完成 `builders.py` `build_site_data` 三個 TPEx/興櫃 highlight 與統計 fallback broad catch 的 debug context 與 logging contract；166 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-19 | ✅完成（第十九子批次） | 完成 `builders.py` `build_site_data` 兩個 same-day activity/intraday fallback broad catch 的 debug context 與 logging contract；167 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-20 | ✅完成（第二十子批次） | 完成 `builders.py` `build_site_data` 三個市場補充資料 fallback broad catch 的 debug context 與 logging contract；168 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-21 | ✅完成（第二十一子批次） | 完成 `builders.py` 剩餘兩個未直接記錄 broad catch 的 debug context 與 logging contract；170 tests、full verify 全綠。builders.py 本輪未直接記錄 broad catch 已完成，H-03 其餘模組仍待後續子批次。 |
| 2026-08-30 | H-03-22 | ✅完成（第二十二子批次） | 完成 fetchers/parsers 各一個剩餘未直接記錄 broad catch 的 debug context 與 logging contract；172 tests、full verify 全綠。H-03 其餘例外治理仍待後續子批次。 |
| 2026-08-30 | H-03-23 | ✅完成（第二十三子批次） | 完成 `app.py` 與 `routes_twse.py` 四個既有 fallback broad catch 的 logging context 與 contract tests；175 tests、full verify 全綠。H-03 runtime production 例外治理完成。 |
| 2026-08-30 | H-04-01 | ✅完成（第一組候選） | 依三方證據移除 `split-02.css` 兩個無引用 `.backtest-signal` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-02 | ✅完成（第二組候選） | 依三方證據移除未載入 `assets/styles.css` 的 `chart-stack`、`chart-area`、`chart-points` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-03 | ✅完成（第三組候選） | 依三方證據移除未載入 `assets/styles.css` 的 `class-hero-value-row`、`class-hero-metrics` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-04 | ✅完成（第四組候選） | 依三方證據移除 `split-03.css` 四個無引用 `chip-summary-card` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-05 | ✅完成（第五組候選） | 依三方證據移除 `split-04.css` 一個無引用 `futures-timeframe-chip` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-06 | ✅完成（第六組候選） | 依三方證據移除 `split-04.css` 七個無引用 `us-architecture-node` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-07 | ✅完成（第七組候選） | 依三方證據移除 `split-05.css` 兩個無引用 `options-heat-cell` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-08 | ✅完成（第八組候選） | 依三方證據移除 `split-06.css` 一個無引用 `options-market-chain-data-table` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-09 | ✅完成（第九組候選） | 依三方證據移除 `split-04.css` 兩個無引用 `us-etf-category-filters` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-10 | ✅完成（第十組候選） | 依三方證據移除 `split-03.css` 兩個無引用 `us-sector-plain-result` 純規則；175 tests、security 16/16、E2E 與 full verify 全綠。 |
| 2026-08-30 | H-04-11 | ✅完成（第一條 JS 呼叫鏈） | 完成 institutional trade 5 函式疑似死碼鏈重評與移除；同步更新 global-symbol baseline；175 tests、full verify 全綠。其餘 4 條鏈仍觀察。 |
| 2026-08-30 | H-04-12 | ✅完成（第二條 JS 呼叫鏈） | 完成 weighted VIX comparison 2 函式疑似死碼鏈重評與移除；同步更新 global-symbol baseline；175 tests、full verify 全綠。其餘 3 條鏈仍觀察。 |
| 2026-08-30 | H-04-13 | ✅完成（第三條 JS 呼叫鏈） | 完成 futures mini sparkline 2 函式疑似死碼鏈重評與移除；同步更新 global-symbol 與 escapeHtml baseline；175 tests、full verify 全綠。其餘 2 條鏈仍觀察。 |
| 2026-08-30 | H-04-14 | ✅完成（第四條 JS 呼叫鏈） | 完成 futures indicator switch 2 函式疑似死碼鏈重評與移除；同步更新 global-symbol 與 escapeHtml baseline；175 tests、full verify 全綠。其餘 1 條鏈仍觀察。 |
| 2026-08-30 | H-04-15 | ✅完成（第五條 JS 呼叫鏈） | 完成 US watchlist 2 函式疑似死碼鏈重評與移除；同步更新 global-symbol 與 escapeHtml baseline；175 tests、full verify 全綠。H-04 五條 JS 死碼鏈全部完成。 |
| 2026-08-30 | H-05-PLAN | ✅完成（設計規劃） | 完成 TD-03 現況、容量、migration、故障模式、部署拓撲與回退方案規劃；未修改程式、部署設定或正式資料。H-05-01～H-05-06 已逐批完成，H-05-07 offline canary preflight 已完成；實連 staging、provider 與 rollout 策略仍待確認。 |
| 2026-08-30 | H-05-01 | ✅完成（觀測與雙 process probe） | 完成 TD-03 state inventory、容量指標與雙 process 可重現探針；未改 production 行為、部署或正式資料；175 tests、full verify 全綠。 |
| 2026-08-30 | H-05-02 | ✅完成（adapter 與 contract tests） | 完成 rate-limit／TTL cache／lease 契約、memory fake、optional Redis adapter 與故障契約測試；未接線 production、未新增 dependency；179 tests（1 skipped）、full verify 全綠。 |
| 2026-08-31 | H-05-03 | ✅完成（rate-limit shadow／Redis feature flag） | 完成 local／shadow／redis 三模式、atomic sliding-window 接線與 Redis fail-closed；預設 local 不變；182 tests（1 skipped）、full verify 全綠。 |
| 2026-08-31 | H-05-04 | ✅完成（single-flight／options lease） | 完成 generic cache-flight 與 TAIFEX options lease feature flag、120 秒 wait／TTL、owner-token fencing 與失效降級；預設 local 不變；187 tests（1 skipped）、full verify 全綠。 |
| 2026-08-31 | H-05-05 | ✅完成（cache L2 feature flag） | 完成 6 個 JSON-safe bucket 的 bounded L1／versioned Redis TTL L2、L1 回填與 failure fallback；預設 local 不變；191 tests（1 skipped）、full verify 全綠。 |
| 2026-08-31 | H-05-06 | ✅完成（background scheduler lease） | 完成 updater lease、120 秒 TTL／renew、owner fencing、contention skip、expired takeover 與 failure skip；預設 local 不變；197 tests（1 skipped）、full verify 全綠。 |
| 2026-08-31 | H-05-07 | 🟡完成（offline canary；其餘封存） | 離線雙 worker shared-state canary、deployment single-worker gate、fault injection 與 `redis==8.1.0` dependency test 全綠；實連 Redis staging、7 個未納入 L2 bucket、provider 與 multi-worker rollout 已依授權封存暫緩。 |
| 2026-08-31 | H-06-PLAN | ✅完成（評估規劃） | 規劃 4 個子批次：資產盤點、21 頁 cold／warm 量測、候選方案成本／收益評估、canary／回退門檻；尚未執行 bundler、CSP 或部署工具鏈變更。 |
| 2026-08-31 | H-06-01 | ✅完成 | 完成 15 JS／7 CSS 靜態資產尺寸、壓縮尺寸、載入順序、頁面覆蓋與 895 個共享全域符號盤點；未改產品或 baseline；175 tests、security 16/16、E2E 與外部連線 full verify 全綠。 |
| 2026-08-31 | H-06-02 | ✅完成 | 新增只讀前端量測腳本；21 頁 cold／warm／refresh、資產 bytes、request count、meaningful-render proxy、cache hit 與代表性互動完成；19 互動成功、2 個空 holdings 前置條件；175 tests、security 16/16、E2E 與外部連線 full verify 全綠。 |
| 2026-08-31 | H-06-03 | ✅完成 | 完成五類候選方案成本／收益／風險矩陣與推薦順序；未改產品／部署／baseline；175 tests、security 16/16、E2E 與外部連線 full verify 全綠。 |
| 2026-08-31 | H-06-04 | ✅完成 | 完成 shadow build、單頁 canary、全頁 rollout gate 與 classic-slice revert 門檻；未執行 bundler／部署變更；175 tests、security 16/16、E2E 與外部連線 full verify 全綠。 |
| 2026-08-31 | H-07-PLAN | ✅完成（規劃） | 完成 TD-19 的 4 批次方案：44 endpoint 欄位政策盤點、schema／failure classification、offline verifier、live/full rollout；本規劃批次未修改 verifier 或 baseline，工作樹既有未提交草稿另待審核。 |
| 2026-08-31 | H-07-01 | ✅完成 | 完成 44/44 endpoint required／optional 欄位政策盤點與 matrix；177 tests、offline quick、security 16/16、E2E 與外部連線 full verify 全綠；未修改 verifier 或 baseline。 |
| 2026-08-31 | H-07-02 | ✅完成 | 完成 allowlist schema、44/44 coverage policy、failure classification 與 12 個測試案例設計；177 tests、offline quick、security 16/16、E2E 與外部連線 full verify 全綠；未擴充 verifier 或 baseline。 |
| 2026-08-31 | H-07-03 | ✅完成 | required-key policy 已接入 offline／quick verifier；44/44 coverage、183 tests、offline quick、security 16/16、E2E 與外部連線 full verify 全綠；未修改產品或 baseline。 |
| 2026-08-31 | H-07-04 | ✅完成 | 完成 live/full rollout 最終核對；44/44 policy、183 tests、offline quick、security 16/16、E2E、21 頁 frontend、94 條 interaction 與外部連線 full verify 全綠；TD-19 結案。 |
| 2026-08-31 | H-08-01 | ✅完成（現況掃描） | AST 重新盤點 H-08 範圍 167 個 broad catch：150 個 production runtime fallback/API error、17 個 regression／工具腳本可接受例外；未發現需另立行為修復候選，並記錄 12 個 TD-03／安全／測試排除項。盤點本身未修改程式、baseline、CSP、部署設定或正式資料。 |
| 2026-08-31 | H-08-02 | ✅完成（單一工具 catch 組） | `portable_check.py` health readiness retry catch 補 attempt／endpoint debug context，新增 mock contract test；176 tests、security 16/16、E2E 與外部連線 full verify 全綠。保留既有 retry／timeout／pass-fail 行為，未更新 baseline、CSP、部署設定或正式資料。 |
| 2026-08-31 | H-08-03 | ✅完成（對帳／狀態更新） | H-08-02 後 AST 對帳維持 167 個 H-08 scope broad catch、12 個排除項；`.gitattributes` 行尾屬性維持。六層驗證與外部連線 full verify 全綠，TD-13 更新為已完成，H-08 結案。 |
| 2026-08-31 | H-09-01 | ✅完成（dynamic candidates 分類） | 重新核對 81 個 dynamic rows 的 JS／HTML 產生路徑、前綴-only 不確定項、21 頁與 94 條互動 manifest；全數保留，未刪 CSS、未更新 baseline／CSP／部署設定／正式資料。完整驗證結果已記錄於本計畫。 |
| 2026-08-31 | H-09-02 | ✅完成（grouped selectors 逐組核實） | 重新解析 247 個 candidate-bearing rule blocks，核對 188 個 mixed grouped blocks、responsive media、active／focus／hover 與 cascade 依賴；全部保留，未刪 CSS、未更新 baseline／CSP／部署設定／正式資料。176 tests、security 16/16、E2E 與外連 full verify 全綠。 |
| 2026-08-31 | H-09-03 | ✅完成（單一候選組刪除） | 依 CSS／HTML-JS／21 頁與 94 條互動三方證據，刪除 `split-03.css` 的 `us-sector-end-label` 3 個 rule blocks；刪除後 176 tests、security 16/16、E2E 與外連 full verify 全綠。未更新 baseline／CSP／部署設定／正式資料。 |
