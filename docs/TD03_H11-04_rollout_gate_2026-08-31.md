# TD-03 H-11-04 rollout gate 與 rollback runbook

日期：2026-08-31  
狀態：🟡 gate 執行完成；rollout blocked，未建立部署變更；TD-03 整體已於 2026-09-01 暫時封存。

## Gate 結果

`regression/td03_h11_04_rollout_gate.py` 為唯讀檢查，輸出
`TD03_H11_04_ROLLOUT_GATE_BLOCKED`。本機目前：

- 通過 safe state：`MARKET_PULSE_RATE_LIMIT_MODE`、`MARKET_PULSE_CACHE_L2_MODE`、
  `MARKET_PULSE_SINGLE_FLIGHT_MODE`、`MARKET_PULSE_BACKGROUND_LEASE_MODE` 均為預設
  `local`。
- 通過 deployment worker gate：`Procfile` 與 `render.yaml` 都是 `--workers 1`，入口仍為
  `app:app`。
- 未滿足：`MARKET_PULSE_REDIS_URL`、Redis PING／sentinel、backup／restore、H-11-02
  real Redis contract、H-11-03 real dual-worker staging load／latency／disconnect evidence、
  approved observation period、manual rollout approval。

因此 `eligible=false`。H-11-01～H-11-03 的 offline 證據不能替代實 staging evidence；不
執行 provider provisioning、不切換 shared modes、不增加 worker、不發佈。

## 未來 rollout batch 的 entry gate

只有下列項目全部有可追溯證據，才可另立部署變更批次：

1. secret store 注入隔離 staging URL，未洩漏 secret；provider、region、成本上限與 namespace
   已核准。
2. PING、短 TTL sentinel、backup／restore、6 個 JSON-safe L2 bucket Redis contract 通過。
3. 雙 worker staging 壓測、p50／p95／p99 latency、timeout／disconnect fault injection、
   rate-limit fail-closed 與三類 lease policy 通過。
4. 完整驗證與 observation window 無未解釋回歸，並取得人工 rollout approval。

該部署批次仍須獨立記錄 diff、環境變更、觀測指標與 rollback owner；本文件不授權未來的
部署操作。

## Rollback 順序

若 staging 或 rollout 發生 shared backend、latency、lease ownership 或 API regression：

1. 關閉所有 shared modes，回到 `local`；
2. 恢復單 worker `--workers 1`；
3. 回退單一 adapter／deployment commit；
4. 保留 failure evidence、backup／restore checkpoint 與 observation 記錄，不修改 SQLite
   或 `twse-cache.json`。

## 本批限制

本批未修改 baseline、CSP、headers、`CACHE_VERSION`、Service Worker、`Procfile`、
`render.yaml`、worker 數、Redis URL、SQLite 或 `twse-cache.json`。

## 驗證

- `TD03_H11_04_ROLLOUT_GATE_BLOCKED`；gate tests 2/2 PASS。
- H-11 shared-state／cache／canary／derivatives unit：207 tests PASS、1 skip（Redis URL
  absent）。
- security 16/16 PASS；`E2E_SMOKE_OK`；`verify_against_baseline.py --full` 已執行，
  外部資料／瀏覽器網路阻擋依規範標記 `[外部問題]`，未更新 baseline。
