# TD-15 R1-01 CSS deletion evidence（2026-08-31）

## Scope

本批依 R0 修正後的現況 manifest，處理 11 個 `candidate-only-review` rule blocks。所有目標都在 `assets/styles.css`；該檔案不是 production split CSS，也不在 `market_config.py` 的 `ROOT_STATIC_FILES`，21 個 HTML 均未載入它。

精確執行規格見 [TD15_R1_01_deletion_specs_2026-08-31.json](TD15_R1_01_deletion_specs_2026-08-31.json)。刪除單位是目前檔案與目前起始行號，不沿用歷史行號。

## Three-way evidence

### 1. CSS static evidence

- R0 manifest current decision：11 個 blocks 全部為 `candidate-only-review`。
- selector 全部為單純 class selector 或同一 selector 的純 class grouping：`.trend-tabs`（兩處）、`.trend-tab`、`.stock-table`、`.table-head, .table-row`、`.table-head`、`.table-row`、`.detail-grid`、`.insight-list`、`.insight-list li + li`、`.detail-link`。
- 不含 live compound branch、`:hover`／`:focus`、`is-*` state、responsive context 或 dynamic-prefix class。
- R0 修正後的 chained-selector parser 已確認 selector 內所有 class token；本批沒有被漏解析的第二 class branch。

### 2. HTML／runtime／static whitelist evidence

- `assets/styles.css` 不存在於 21 個 HTML 的 stylesheet links；實際載入的是根目錄 `styles.css` 及 `split-01.css`～`split-07.css`。
- `market_config.py` 的 `ROOT_STATIC_FILES` 只列根目錄 `styles.css` 與七個 split CSS，沒有 `assets/styles.css`。
- R0 literal scan 的 11 個候選 blocks 無 HTML／JS／Python direct usage hit；R0 掃描範圍排除 docs、baseline 與 `.tmp`，避免證據被文件本身污染。

### 3. Page／interaction evidence

- Scope 固定為 21 頁、94 條 interaction steps。
- 因目標 stylesheet 未被 production page 載入，刪除不改變目前頁面 cascade；不更新 baseline。
- 若驗證出任何未分類 DOM／pixel／interaction 差異，立即停止後續 TD-15 批次並以本批反向 diff 回復。

## Target blocks and R0 hashes

| current file | current lines | selector | R0 SHA-256 |
|---|---:|---|---|
| `assets/styles.css` | 836–840 | `.trend-tabs` | `113b1b8e41ea7ba148d5088317f21ff18d03226dde09858f59a25461e7680441` |
| `assets/styles.css` | 842–851 | `.trend-tab` | `5447c47cc40b53869704691d36fd1fca101403001d2e05fb534ae4eaed9f1ebb` |
| `assets/styles.css` | 965–967 | `.stock-table` | `a5dc0de7623f6b1b53f2af6f483ff78e98c6d427ba9e86787abe8f46674709c0` |
| `assets/styles.css` | 969–976 | `.table-head, .table-row` | `28af4f584e8f213c75a3a22896b824c1e995f7cc1ec854e1c0b8a26ad98ea863` |
| `assets/styles.css` | 978–981 | `.table-head` | `10e5d98b0c4f50a4340e317213dd061a730d9403dde6c2481328466525192079` |
| `assets/styles.css` | 983–985 | `.table-row` | `2754ddc31a67b8616df17227865bd64cdbb0f533b7406f1c24244e78c568f5d4` |
| `assets/styles.css` | 987–989 | `.detail-grid` | `ddb7884ff5e4c1a727dc68213ab261c176dae3ac581a44c5ce5c899d1c4f3005` |
| `assets/styles.css` | 1003–1006 | `.insight-list` | `147c5160150d8967e7c1ed59a736cfa3c8332b380bb2f944efd2bee9baa012d0` |
| `assets/styles.css` | 1008–1010 | `.insight-list li + li` | `c3e1cad68661a8816e13e60576c5f177d5cf08c68ed0f1d8ff90b39ee27c522f` |
| `assets/styles.css` | 1430–1435 | `.detail-link` | `10c28884f26099a40f4b1bbea863c93e1863d55f61b22a42c7ff90594609c608` |
| `assets/styles.css` | 1473–1475 | `.trend-tabs` in media block | `abb2f070f01fc1f570b85578e5f60edd1a5bf129c2d6106b3ecbb9991be3a42d` |

## Execution gate

- Batch size：11 rule blocks（≤20）。
- Target：only `assets/styles.css`; no HTML／JS／Python／baseline modification.
- Rollback：反向套用本批 `assets/styles.css` diff 即可完整恢復；不回退其他 dirty worktree 變更。
- 本文件在刪除前建立；刪除與驗證結果如下。

## Execution result

- `delete_css_rules.py` 成功刪除 11 個 rule blocks，目標檔案只有 `assets/styles.css`。
- 刪除後重建 R0 manifest：271 個含候選 class 的 rule blocks、0 個 `candidate-only-review`；剩餘 198 個 dynamic/status/direct-reference 與 73 個 grouped/compound blocks，不在本批刪除範圍。
- `python -m py_compile regression/td15_residual_inventory.py regression/delete_css_rules.py`：通過。
- `python -m unittest test_derivatives_platform.py`：176 tests 通過。
- `python security_guardrail_check.py`：16/16 通過。
- `python e2e_smoke.py`：`E2E_SMOKE_OK`。
- `python regression/verify_against_baseline.py --full`：`VERIFY_OK`，21/21 frontend、94/94 interaction；TAIFEX/Yahoo `WinError 10013` 為外部連線警告。
- 未更新 baseline、HTML、JS、Python、CSP、headers、Service Worker、cache、部署設定、SQLite 或 `twse-cache.json`。

## 後續 closure（2026-09-01）

本批 11 個刪除結果已納入後續 R0／REMAIN current inventory。REMAIN-04～07 已完成其餘 263 個現況 blocks 的逐項 review；本批歷史刪除不代表後續 blocks 一律刪除，最終 closure 為 263 個保留、沒有新的 deletion batch。
