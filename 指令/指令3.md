TD-09 說明確認通過。兩個收尾動作:

1\. 將「前後端相依的破壞性變更部署時,需一併遞增

CACHE\_VERSION 強制全清」寫入部署注意事項

(README 或 CLOUD\_DEPLOYMENT.md)

2\. CLAUDE.md 規則異動與本說明一併 commit

  

完成後開始 TD-05/12 fetcher registry:

TD-09 說明已確認。開始 TD-05/12:fetcher registry。

  

目標:86 個 fetch\_\* 收斂為宣告式 source registry

(URL / headers / parser / TTL 設定化)+ 單一 HTTP client;

25 個 TTL 常數一併收攏,快取加上限 LRU。

  

規則:

1\. 本工單屬「行為不變」型:對外輸出、快取語意、錯誤處理

行為必須與現狀一致,registry 只改組織方式不改行為。

2\. 先提分批收斂計畫(Plan mode),每批 8-12 個函式,

計畫需說明 registry 的資料結構設計,經我確認後執行。

3\. 每批五層驗證全綠 + 獨立 commit;patch 目標修正

沿用既有檢查模式。

4\. 之前標記 DEADCODE-CANDIDATE 的函式,在對應批次

提出裁決建議(刪除/保留+理由),由我確認後執行。

5\. LRU 上限屬行為微調,各快取的上限值在計畫中列出

供我確認,不自行決定。

4\. 之前標記 DEADCODE-CANDIDATE 的函式,在對應批次

提出裁決建議(刪除/保留+理由),由我確認後執行。

5\. LRU 上限屬行為微調,各快取的上限值在計畫中列出

供我確認,不自行決定。
