分類抽查通過,兩份表證據充分,可以進入刪除。三個裁決:

  

1\. 第一層 52 個 JS:核准刪除。但刪除前先建 git tag

pre-td15-deletion 作為整批回退錨點(52 個雖各批 commit,

但這是本輪唯一的大量刪除,留一個乾淨的整體回退點)。

依工單分批(≤20)刪除,每批 verify --full。

  

2\. 第二層 6 個:核准標記觀察。但因為你發現「整串可能都是

死的」,在 observe list 中額外記錄這個發現——註明這 6 個

與其呼叫者形成一個「疑似封閉死碼團」,下一輪應整團一起

重新評估(而非逐個),並列出完整的呼叫關係圖。

console.warn 這輪先不加(避免動到行為),僅加註解。

  

3\. CSS 135 個靜態 class:核准刪除,分批 verify --full,

pixel diff 零差異是硬門檻。7 個候選 ID 既然是報告誤判、

根本不是真死碼,不列入刪除,並在 css\_unused\_report.md

標註更正原因。

  

執行順序:先 JS 第一層(第二部分)→ 再 CSS 第三層(第四部分)。

兩者分開,不要混在同一批 commit,萬一要回退能分別處理。
