那兩個 NameError 不是既存問題,是 Slice 4 引入的回歸

(原版的 app.X 引用是有效的,搬移後才斷)。

依「行為不變」原則應修復而非保留:

1\. 修正 fetchers.py 中兩處失效引用,指向 builders.py 的新位置

2\. 全 repo 掃描所有 deferred app.X / 跨模組延遲引用,

確認沒有第三個同型潛伏斷點,掃描結果列出

3\. 為這兩條路徑補上最小觸發測試(防止再斷無警報)

4\. 五層驗證後以 fix(regression) 獨立 commit

完成後 TD-01 才正式結案。
