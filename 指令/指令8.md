盤點確認通過。發現①屬重大修正,依此變更執行方案:

  

【方案變更】撤銷工單第 2 節的 window.MP 命名空間方案。

理由:classic script 的頂層 function 與 let/const 為所有

script 共享,拆檔後跨檔引用天然有效,加命名空間反而需要

改寫數千個呼叫點,是不必要的高風險機械修改。

  

新規則:

1\. 符號維持頂層宣告,不包裝、不改任何呼叫點。

每個批次為純粹的「剪下 → 貼上 → 調整 HTML script 順序」。

2\. 每個符號全專案剛好宣告一次(let/const 重複宣告會直接

SyntaxError,列入第 6 節驗證)。

3\. 載入時初始化檢查(新增,重要):確認沒有任何先載入檔案的

「頂層執行程式碼」呼叫到後載入檔案宣告的函式。

特別檢查 state.js 的初始化式與 core.js 的

enforceSafeInnerHtml() IIFE。

4\. 全域污染檢查改為:拆分前後的全域符號集合必須完全相同

(window 自有屬性 + 全域詞法綁定探測),不是「只允許 MP」。

  

【五項裁決】

Q1 接受合併,但 page-global-market 物理上切成三個檔案

(-futures / -options / -assethub),文件註明為同一邏輯模組。

Q2 接受 22 批,不放寬單批上限。

Q3 98 個未歸類符號集中至 js/legacy-unclassified.js 隔離檔,

不刪除、不分散;完整清單附入 docs/TD稽核清單.md 的 TD-15

作為靜態分析補遺,標註為低信心待核實。

Q4 derivatives-ui.js 不納入本工單。

Q5 CSS 切分留到各批次當下做,累積寫入 docs/css\_split\_map.md。

  

同步更新工單檔第 2 節與第 6 節後 commit,再開始批次 1。
