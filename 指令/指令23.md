TD-17 方向確認:採方案 A,但補強你自己指出的缺口。

  

核准:

1\. check\_api\_baseline 開始比對前先做一次明確的預熱

(ensure\_cache 等級),給寬鬆 timeout;預熱失敗就提早

明確報「外部資料源不可用(非程式碼問題)」而中止,

不要讓 44 個端點各自逾時後回含糊紅燈。

2\. live-sectors/live-overview 這兩個「設計上必打外部、

無快取」的端點移出 --quick,改列入新的 --api-live 旗標。

  

補強要求(針對你指出的缺口):

3\. --api-live 不是可有可無的:把它加進 --full 的組成,

讓完整驗證仍然涵蓋這兩個即時端點的程式碼正確性。

結果是:--quick 快而穩(只驗有快取的端點),

\--full 完整(含即時端點),兩者分工明確。

4\. 預熱步驟與比對步驟的「失敗訊息」必須可區分:

\- 預熱/連通性失敗 → 明確標示「外部問題,非程式碼」

\- schema 比對失敗 → 明確標示「程式碼可能改動了回應格式」

這是 TD-17 的核心目的——讓紅燈永遠能一眼判讀是哪一種。

  

另外兩項要求:

5\. 更新 README/工單00 文件:--quick 的預期耗時、

新的 --api-live 旗標用途、--full 的組成。

6\. 這是改動驗證工具本身:改造完成後,故意注入一個

「真的改壞 API 回應格式」的變更,確認改造後的 --quick

仍然抓得到(schema 比對 FAIL),證明修穩定性沒有

犧牲偵測力。驗證後還原。

  

以上為行為變更型工單,分批 commit,每批不影響其他驗證層。
