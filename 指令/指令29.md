四項確認。兩個編號釐清:

TD-22 = guardrail 在 portable 環境的 SKIP 機制

TD-23 = app.py:117 import-time 建庫副作用

請確認 docs/TD稽核清單.md 兩筆都在、描述正確、無編號重複。

  

另外確認一件事(F-02 相關):

build\_portable\_package.py 產出的包,經過 F-03 的

clean-package 驗證後,交付包目錄是否確實回到乾淨狀態

(那個 import 副作用產生的 .sqlite3 已刪除、

release\_manifest.json 的逐檔 SHA 仍全部自洽)?

請實測並回報——這是「交付包能被獨立覆核」的最後一哩。
