三項全部確認通過,裁決如下:

1\. SourceSpec 設計核准,fallback 排除於 registry 的

邊界決定明文寫進 fetch\_registry.py 的 module docstring。

2\. Finding B 核准,按行為不變處理。

3\. LRU 數值表核准,live\_search\_dedup 按行為微調

獨立 commit 並在 message 標註。

追加兩個要求:

a. LRU 機制在 cache.py 實作一份共用,13 個 bucket

套用同一機制,不做 per-bucket 複製。

b. Batch 1 一併建立 registry 不變量測試:

SourceSpec name 唯一、URL 非空、parser 可解析、

ttl\_seconds 與具名常數一致——當作 registry 的

自我稽核,納入五層驗證。

4\. 死碼四項方向同意,Batch 12 執行前再逐一過我確認。

  

開始 Batch 1。
