TD-16 修復方向確認:

  

【核心修復】同意。終止條件改為「已取得明確結果(含 0 筆)

即不再重新觸發載入」,根治「資料不足」與「尚未載入」混淆。

這是本次 bug 的根本,務必修在這裡,不要只在筆數判斷上補綴。

  

【顯示方案】採選項 3(混合),但加兩個約束:

1\. 對「筆數 >= 5」的正常情況,渲染輸出必須與修復前

逐位元組一致——這是絕大多數股票的路徑,不容許因為

改 <5 邏輯而順手動到正常顯示。用一檔正常股(2330)

的 golden 輸出比對確認。

2\. <5 筆時各聚合窗口的「資料不足」呈現,沿用頁面既有的

同類提示樣式/文案(例如其他資料缺漏時已有的處理方式),

不新造 UI 元件、不加新 CSS class。若既有頁面沒有可沿用

的樣式,先回報,不要自行設計。

  

【防遞迴的保險】除了修終止條件,額外加一道硬上限:

loadInstitutionalTradeHistoryIfNeeded 對同一 stockCode

在單次 renderStockDetail 生命週期內最多觸發一次

(用一個 in-flight / already-attempted 標記)。理由:終止

條件是邏輯防線,這道是結構防線——即使未來有人改壞終止

條件,也不會再退化成無限遞迴當機。兩道一起上。

  

確認後執行第二部分,含解除三處 known-broken、基準 93→94。
