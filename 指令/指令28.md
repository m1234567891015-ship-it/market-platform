claude "請讀取 docs/TD-RELEASE-v2\_執行指引\_供ClaudeCode.md，

按文件建議順序執行：F-04 → F-01 → F-02 → F-03。

  

每一項都嚴格照 characterize → rewrite → prove equivalence 三階段進行，

不要跨項目一次做完才驗證：

  

1\. characterize：先跑文件裡列出的查證指令

（例如 git log --all -- build\_portable\_package.py、

git log --all -- tests/fixtures/），

把結果印出來給我看，不要假設答案。

2\. rewrite：只在 characterize 結果確認後才動手改，

遇到 F-02 這種『repo 歷史也從未存在過』的情況，

停下來跟我確認要當新開發項目還是維持原優先度，

不要自行降級處理。

3\. prove equivalence：跑完文件裡指定的驗收指令

（115 tests、clean-package 驗證、bundle clone 驗證等），

貼出實際輸出，不要只回報『應該會過』。

  

每完成一項（F-04、F-01、F-02、F-03）就個別 commit，

commit message 標明對應的 TD 編號，不要合併成一個大 commit。

四項全部完成前，不要更新 README 或宣稱 conditional\_pass 解除。"
