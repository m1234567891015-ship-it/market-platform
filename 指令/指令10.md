批次 2 確認,繼續批次 3。兩項規則追加:

  

1\. 逐位元組比對升格為每批強制步驟(不只批次 2 剩餘部分):

每批搬移後,對被搬移的每個符號做 app.js 原始位元組

與新檔案位元組的逐一比對,確認完全相同。

理由:regex/樣板字面值/跳脫序列的靜默改寫,四層護欄

全部攔不到,只有位元組比對抓得到。

  

2\. 修正 escapeHtml 計數檢查:

a. 計數來源改為 app.js + js/\*.js 合併(與 security\_guardrail\_check

找函式定義的修法一致),不再只讀 app.js。

b. 判定改為「精確等於基準 2037」,移除 95% 門檻。

純搬移工單不應容許任何呼叫點消失。

c. 先確認目前 2035 的 2 個差額,逐一指出是哪兩處、

在合併計數下是否回到 2037。

  

另:後續批次不需每批停下等我確認,依工單第 7 節的檢查點

即可(page-\* 全部完成、renderStockDetail 批次、全案完成)。

若連續兩批需要修改框架或既有檢查,則停下讓我複核。
