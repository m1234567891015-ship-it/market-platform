# Market Pulse 技術債改善計畫

版本：V1  
規劃日期：2026-08-29  
依據：[技術債與系統檢驗報告_2026-08-29.md](./技術債與系統檢驗報告_2026-08-29.md)

## 1. 目標

本計畫的目標不是一次性重寫，而是以可驗證、可回退的小批次方式：

- 先恢復 TWSE/API 與前後端連線的可觀測性及可用性。
- 消除使用者可感知的前端當機與失效控制項。
- 讓完整回歸測試能區分「程式錯誤」與「外部資料源不可用」。
- 降低單進程記憶體狀態、前端大型檔案、快取分散與測試副作用風險。
- 保留既有 API 回應格式、頁面行為與安全護欄。

## 2. 執行原則

1. 一次只處理一個獨立批次；完成後跑完整六層驗證再進入下一批。
2. 任何使用者可見行為、API schema 或驗證基準變更，先提出方案並確認後才實作。
3. 不直接修改或重建 golden baseline；若行為變更是任務目標，需獨立提交基準更新。
4. 外部資料一律提供明確的 timeout、錯誤碼、fallback 與 log context。
5. 測試使用獨立 temporary cache/database，不改動正式執行期資料。
6. 每個批次保留可獨立回退的 commit，避免大型混合提交。

## 3. 優先級總表

| 優先級 | 批次 | 主要項目 | 預期結果 |
|---|---|---|---|
| P0 | A | TWSE/API 500/502、外部連線診斷 | 明確知道是產品錯誤、資料源錯誤或環境阻擋 |
| P1 | B | TD-16、TD-15 使用者可感知前端問題 | 不再遞迴當機；失效按鈕有正確行為或移除 |
| P1 | C | 離線 fixture 與驗證工具可靠性 | `--quick` 可重現，不受即時服務波動誤導 |
| P1 | D | 多 worker 與快取狀態 | 部署限制明確，快取/限流行為可驗證 |
| P2 | E | 前端導覽、資源與 CSS 整理 | 降低載入量與維護成本 |
| P2 | F | 測試副作用與 import 副作用 | 測試/驗證不污染交付包 |
| P3 | G | 例外處理、重複與大型函式 | 持續降低維護複雜度 |

## 4. 分階段改善計畫

### 批次 A：API 與外部資料源可用性（P0）

對應：`/twse-data.js` 500、Stock search API 500、`/api/twse/site-data` 500、三個 TWSE live API 502。

工作內容：

- 以獨立 server log/trace 追查每個 500 的實際 exception、輸入參數與 fallback 分支。
- 確認 `twse-data.js` 靜態路由是否被錯誤導向動態處理器。
- 檢查 Stock search 的空查詢、無結果、錯誤資料格式與外部服務 timeout。
- 對 TWSE live API 統一錯誤 response 結構，至少包含 `error_code`、可供前端顯示的安全訊息與 request context。
- 增加不依賴外網的成功、空資料、上游失敗三組 fixture 測試。

完成標準：

- 三個明確 500 問題在允許外部連線的環境不再重現，或已被定位為上游服務問題並有正確 fallback。
- API error response 不洩漏 traceback、URL secrets 或內部路徑。
- 既有 API schema 無非必要變更。
- `portable_check.py` 通過。

回退策略：只回退本批次 API handler/fetcher commit；保留診斷 log 與 fixture。

### 批次 B：前端使用者可感知錯誤（P1）

對應：TD-15、TD-16。

工作內容：

- 為法人歷史少於 5 筆、0 筆、上游失敗建立固定測試資料。
- 為 `loadInstitutionalTradeHistoryIfNeeded` 加入明確的重試上限/終止條件，禁止無條件重新渲染遞迴。
- 驗證個股詳情頁在資料不足時仍可顯示其他區塊與可理解的空狀態。
- 核對 `[data-asset-option-underlying]` 控制項：若功能保留則修復可達路徑；若刻意停用則移除按鈕或顯示 disabled/未提供原因。
- 補充互動測試，至少覆蓋成功、空資料、API 失敗三種狀態。

完成標準：

- 不會出現 `RangeError: Maximum call stack size exceeded`。
- 所有可見且可點擊控制項都有成功、失敗或明確 disabled 行為。
- 相關頁面互動測試通過，且無新增 console error。

回退策略：前端修改以單一功能 commit 提交；若畫面行為不符預期，只回退該功能 commit，不更新 baseline。

### 批次 C：建立可信的離線驗證層（P1）

對應：TD-17、TD-19，以及完整基準受外部網路影響的問題。

工作內容：

- 將 API 檢查分成 `offline fixture` 與 `live health check` 兩種模式。
- `--quick` 預設只使用 fixture/mock，不直接依賴 TWSE、TAIFEX、Yahoo。
- `--api-live` 或獨立 health check 才執行外部連線，並將上游不可用標記為 external failure。
- 為重要 API 欄位建立 required-key allowlist；時變欄位維持寬鬆比對。
- 對瀏覽器檢查區分本地 server 失敗、外部資源失敗、畫面真正差異。

完成標準：

- 禁止外網時，`--quick` 仍可穩定通過。
- 關鍵欄位缺失時 fixture 測試必須失敗。
- live check 能清楚列出受影響的資料源，不把 external failure 誤報成程式碼回歸。

回退策略：保留舊驗證命令作為 compatibility mode，先新增模式再切換預設值。

### 批次 D：部署狀態與快取一致性（P1）

對應：TD-03、TD-12。

工作內容：

- 確認 `Procfile`/部署設定明確維持單 worker，並在文件說明原因。
- 盤點 rate limit、in-flight lock、memory cache 的生命週期、上限與失效行為。
- 將快取 TTL 與大小限制集中到單一設定/抽象層。
- 為多 worker 不支援情境加入啟動檢查或警告，避免使用者誤部署。
- 評估長期將共享狀態外移至 Redis/SQLite；此項需另提設計方案，不在第一批直接切換。

完成標準：

- 部署文件、Procfile 與實際啟動參數一致。
- 所有主要 memory cache 有 TTL 與 bounded eviction 證據。
- rate limit 行為在單 worker 測試中穩定，並明確記錄多 worker 的限制。

回退策略：先做文件/檢查與低風險快取整理；外部狀態儲存切換必須獨立 feature flag 與 migration plan。

### 批次 E：前端資源與頁面一致性（P2）

對應：TD-08、TD-09、TD-14，以及前端大型函式維護成本。

工作內容：

- 統一 21 個頁面的 nav 來源，先以 build-time 產生或共用 partial 方案評估。
- 確認 `derivatives-ai.html` 是否應完整載入共用資源與導覽。
- 量測 15 個 JS 切片、`app.js`、7 個 CSS 切片的實際頁面載入量。
- 在不改變 cascade 順序的前提下，逐批移除確定未使用的 CSS/JS 死碼。
- 評估 service worker 是否重新啟用；若啟用，同步處理 `CACHE_VERSION` 與破壞性部署策略。

完成標準：

- 導覽連結與 active state 在 21 頁一致。
- 不因 CSS split 調整造成 baseline screenshot 回歸。
- 明確量化初始下載量、快取命中率與頁面載入改善。

回退策略：每次只處理一組頁面或一個 CSS/JS slice；任何截圖差異先回退程式，不直接覆蓋基準。

### 批次 F：測試與交付包無副作用（P2）

對應：TD-23、TD-24。

工作內容：

- 將 `DERIVATIVES_STORE.initialize()` 從 module import 副作用改為明確啟動流程，先確認所有 import caller。
- 測試統一使用 temporary cache/database。
- clean-package 驗證採用 `PYTHONDONTWRITEBYTECODE` 或隔離 pycache 位置。
- 驗證前後檔案雜湊時，明確區分允許的測試產物與不應被修改的交付內容。

完成標準：

- 單純 import `app.py` 不會在工作目錄建立 SQLite。
- clean-package 驗證後不新增 `.pyc`，不修改正式 `twse-cache.json`。
- release integrity 在 existing database 與 clean package 兩模式均通過。

回退策略：先保留舊初始化路徑於啟動入口，確認所有測試/部署 caller 後再移除 import side effect。

### 批次 G：品質與複雜度治理（P3）

對應：TD-07、TD-10、TD-11、TD-13，以及 TD-05/TD-01 後續整理。

工作內容：

- 為 top-10 大型 builder/fetcher 補特性測試與 fixture。
- 確認並消除 `worker` 同名函式遮蔽。
- 對靜默 `except Exception` 補最低限度 debug context，避免無差別吞錯。
- 收緊 SSL fallback 的 opt-in 與每次 fallback audit log。
- 以小批次方式拆分前端大型 render function，維持 classic-script 載入順序。

完成標準：

- 大型函式有可重現的輸入/輸出測試。
- SSL fallback 只有明確允許時才啟用，production 仍被阻擋。
- 不新增 global symbol collision，安全護欄維持全綠。

## 5. 每批次必跑驗證

每一個程式碼批次完成後，依序執行：

```text
1. python -m py_compile <changed .py>
2. node --check <changed .js>
3. python -m unittest test_derivatives_platform.py
4. python security_guardrail_check.py
5. python e2e_smoke.py
6. python regression/verify_against_baseline.py --full
```

另依批次增加：

- API 批次：`python portable_check.py`、fixture API tests。
- 前端批次：21 頁 frontend compare、94 條 interaction compare。
- 交付批次：`python verify_release_integrity.py`，並確認工作樹與執行期資料未被污染。

## 6. 完成定義（Definition of Done）

本專案只有同時滿足以下條件，才能宣稱本輪改善完成：

- P0/P1 問題有明確修復或有可驗證的外部依賴說明。
- 單元測試、安全護欄、E2E、完整基準與必要的 portable/release 檢查全數通過。
- 前端 21 頁與 94 條互動沒有未分類的 console error 或功能失效。
- API 500/502 已消除，或被明確歸類為上游不可用且 fallback/監控可接受。
- TD-15、TD-16、TD-17、TD-19、TD-23、TD-24 有測試證據，不只更新文件狀態。
- 每個修改批次有獨立 commit、驗證紀錄與回退方式。

## 7. 建議執行順序

建議依序執行：**A → B → C → D → F → E → G**。

理由是先處理 API/外部依賴與使用者可感知錯誤，再建立可靠護欄；在護欄可信後，才進行前端資源與複雜度整理，降低誤判回歸的風險。

## 8. 目前暫不處理事項

- 不直接重寫整個 `app.js` 或重新引入 ES module bundler。
- 不在沒有容量、備份、migration 與回退方案前切換 Redis/SQLite 共享狀態。
- 不因本次外部網路阻擋而更新 screenshot、API 或 interaction baseline。
- 不順手清理與當前批次無關的死碼或部署設定。

## 9. 批次執行紀錄

### 2026-08-29：批次 A 完成

完成項目：

- 冷快取刷新失敗時，`/api/twse/site-data`、`/twse-data.js`、`/api/twse/search`、個股/ETF/全部股票等快取型端點改回傳統一的 502 API error envelope，不再落成 Flask 500。
- API error envelope 保留 `error_code` 與安全訊息，新增 `request_context`（path、method、parameter names）；server log 記錄安全化的輸入參數與例外 traceback，敏感參數會遮罩。
- Stock search 對空查詢、無結果、非 list/非 dict/缺少 code 或 name 的快取資料採安全空結果處理，避免格式錯誤造成 500。
- `portable_check.py` 可辨識指定資料端點的標準外部失敗 fallback；外網不可用時仍驗證本地頁面、資源、health 與錯誤信封，不誤報成產品故障。
- 增加 success、empty、invalid-data、upstream-failure fixture 測試；未修改任何 baseline/golden/screenshot/interaction manifest。

驗證結果：

- Python 語法：PASS（`app.py`、`routes_twse.py`、`parsers.py`、`portable_check.py`）。本批次未修改 JavaScript，未執行 `node --check`。
- Unit：PASS，118 tests。
- Security guardrail：PASS，16 項。
- `portable_check.py`：PASS；偵測到 `/twse-data.js` 與 `/api/twse/search?q=2330` 為上游不可用的安全 fallback。
- `e2e_smoke.py`：PASS（`E2E_SMOKE_OK`）。
- `regression/verify_against_baseline.py --full`：退出碼 1；所有失敗均為 `[外部問題,非程式碼]`，原因是目前環境阻擋 TWSE/TAIFEX/Yahoo（`WinError 10013`、`ERR_NETWORK_ACCESS_DENIED`），沒有 `[程式碼可能改動回應格式]`。
- `git diff --check`：PASS；正式執行期 cache/database 未納入變更。

下一步：

- 進入批次 B（TD-16、TD-15 使用者可感知前端問題）：先處理法人歷史資料不足造成的遞迴與失效控制項，再補成功/空資料/API 失敗互動 fixture；不更新 baseline，除非行為變更經確認為任務目標。
- 若要在允許外部連線的環境完成批次 A 的 live/API full 綠燈，重跑 `python regression/verify_against_baseline.py --full`，確認上述三個 live API 與冷快取暖身不再被上游阻擋。

### 2026-08-29：批次 B 完成

完成事件：

- TD-16：保留並驗證 `loadInstitutionalTradeHistoryIfNeeded` 的結構性 circuit breaker 與「只要收到 `rows` 陣列即視為 definitive result」終止條件；0、2、4、缺少/null、5、10 筆資料六種情境均能完成個股詳情渲染，不再遞迴當機或 hang。
- TD-16：補強 `regression/reproduce_td16_recursion.py` 的隔離 fixture，固定 mock live-search、stock-detail 與 `twse-data.js`，避免驗收被無關外部資料源阻擋。
- TD-15：修復 `derivatives-assets.html` 因 `renderAssetHubPage()` 提早 return 而無法到達的 `[data-asset-option-underlying]`；控制項現在實際渲染於公開選擇權卡片，並共用頁面綁定流程。
- TD-15：公開選擇權控制項加入 idle/loading/ready/error 可見狀態；API 失敗時恢復按鈕可用，保留重新嘗試路徑，不輸出未處理錯誤內容。
- TD-15：新增 `regression/reproduce_td15_option_control.py`，覆蓋 5 個標的控制項、QQQ 成功切換與 NVDA API 失敗後的可重試狀態。
- 未修改 API/golden/screenshot/interaction baseline；新增的選擇權卡片是本批次目標中的預期畫面行為變更。

驗證結果：

- Python 語法與 JavaScript 語法：PASS（所有本批次修改的 `.py`，以及 `js/page-global-market-assethub.js`）。
- Unit：PASS，118 tests。
- Security guardrail：PASS，16 項；escapeHtml 呼叫數維持基準 1905，top-level global symbol 未增加。
- `regression/reproduce_td16_recursion.py`：PASS，6/6 情境無 crash/hang。
- `regression/reproduce_td15_option_control.py`：PASS，5 個控制項；QQQ success、NVDA failure/retryable state 均通過。
- `e2e_smoke.py`：PASS（`E2E_SMOKE_OK`）；執行期間外部 TAIFEX/Yahoo 連線仍受環境 `WinError 10013` 阻擋並產生既有警告。
- `git diff --check`：PASS。
- `regression/verify_against_baseline.py --full`：退出碼 1。API live 檢查與 21 頁/94 條互動中的失敗均標示為目前環境的 `ERR_NETWORK_ACCESS_DENIED`/上游不可用；`derivatives-assets.html` 另有 1 項預期結構差異（baseline 31 個 cards、現況 32 個，來自本批次新增公開選擇權卡片）。未發現 `[程式碼可能改動回應格式]`，亦未更新 baseline。

下一步：

- 進入批次 C（TD-17、TD-19）：建立可信的離線 fixture 驗證層，將 `--quick` 與 live 外部檢查分離，並讓前端/API 驗證在禁止外網時仍能穩定區分產品錯誤與 external failure。
- 在允許外部連線的環境重跑批次 B 的 full baseline；若要接受新增卡片的 screenshot/結構差異，需另行確認後以獨立 baseline 更新提交處理。

### 2026-08-29：批次 C 完成

完成事件：

- TD-17/TD-19：新增 `regression/offline_fixtures.py`，以 baseline manifest/API fixture 建立 loopback replay server；structure-only schema 會 materialize 成維持原 schema 的 JSON 值，不需外部服務即可重現比對。
- `verify_against_baseline.py --quick` 改為只驗證離線 fixture，不再啟動正式 app、暖身 `/api/twse/site-data` 或呼叫 TWSE/TAIFEX/Yahoo；`--api-live` 與 `--full` 仍保留 live API 檢查。
- 新增重要 API required-key allowlist；缺少必要欄位會以 `[程式碼可能改動回應格式]` 明確失敗，不被空殼或錯誤資料誤判為成功。
- 新增 `regression/test_offline_verifier.py`，覆蓋 offline quick replay 與必要欄位缺失負向測試；修正 HTTPError fixture response 關閉，避免驗證測試產生 ResourceWarning。
- `frontend_check.py` 與 `interaction_check.py` 將 `ERR_NETWORK_ACCESS_DENIED`、DNS/離線等外部資源錯誤獨立列為 `[EXTERNAL]`；仍保留真正的 console error、元素數量與畫面差異失敗判定。
- 未修改任何 baseline/golden/screenshot/interaction manifest。

驗證結果：

- Python 與 JavaScript 語法：PASS（所有本輪修改的 `.py`；既有批次 B 的 `js/page-global-market-assethub.js` 亦通過 `node --check`）。
- Unit：PASS，118 tests。
- Security guardrail：PASS，16 項；escapeHtml 1905 次、global symbol 與 CSP 護欄均維持。
- `python regression/verify_against_baseline.py --quick`：PASS（`VERIFY_OK`，離線 API baseline 通過）。
- `python -m unittest regression/test_offline_verifier.py`：PASS，2 tests。
- `e2e_smoke.py`：PASS（`E2E_SMOKE_OK`）；外部 TAIFEX/Yahoo 仍受 `WinError 10013` 阻擋並產生既有警告。
- `git diff --check`：PASS。
- `regression/verify_against_baseline.py --full`：退出碼 1；快取型 API PASS、interaction compare PASS；live API 明確分類為 external，21 頁瀏覽器外部資源明確列為 `[EXTERNAL]`，截圖差異列為 `[畫面差異]`。另有 `derivatives-assets.html` baseline 31/現況 32 cards 的批次 B 預期 UI 差異；未發現 API schema 或安全護欄退化。

下一步：

- 進入批次 D（TD-03、TD-12）：盤點多 worker 下的 rate limit、in-flight lock、memory cache 生命週期與 bounded eviction，確認部署單 worker 限制與快取一致性。
- 在允許外部連線的環境重跑 `python regression/verify_against_baseline.py --full`，確認 live API 與瀏覽器資源分類後的剩餘畫面差異；不因本次外部阻擋更新 baseline。

### 2026-08-29：批次 B/C 殘餘項目處理完成

完成事件：

- 依使用者授權，在允許外部連線的環境重跑完整驗證；live API 全數 PASS，未再重現 TWSE/TAIFEX/Yahoo 的外部 API 阻擋。
- `derivatives-assets.html` 的 cards 已由 baseline 31 更新為本批次預期的 32；只更新該頁 screenshot 與 frontend manifest 頁面計數，未更新其他 baseline。
- 更新後完整驗證確認 interaction compare PASS、API schema 無退化、安全護欄 PASS；批次 B 的預期 UI baseline 差異已消除。

驗證結果：

- `python regression/verify_against_baseline.py --full`：外部 API、cached API、interaction compare、安全護欄均 PASS。
- 完整驗證目前僅剩 `derivatives-ai.html`（23.15%）與 `derivatives-analytics.html`（21.97%）兩項動態畫面差異；兩者不是外部 API/資源阻擋，也不是批次 B 的 `derivatives-assets` 變更，未擴大範圍重寫其 baseline。

下一步：

- 進入批次 D（TD-03、TD-12）。若要讓 full verify 連兩個動態頁面也完全綠燈，另立畫面差異調查/基準決策，不與本次外部阻擋及批次 B baseline 處理混併。

### 2026-08-29：批次 D 完成

完成事件：

- TD-03：保留 `Procfile`、`render.yaml` 的 `gunicorn --workers 1 --threads 4` 部署契約；新增明確偵測到多 worker 時的啟動警告，指出 rate-limit、memory cache 與 in-flight lock 仍為 process-local，避免誤部署後悄悄失去跨 worker 一致性。
- TD-03：`API_RATE_LIMIT_STATE` 新增 `MARKET_PULSE_API_RATE_LIMIT_MAX_CLIENTS` 上限（預設 10,000），在滑動視窗清理時淘汰最舊 client，避免不可信 client identity 令記憶體無界成長。
- TD-12：在 `market_config.py` 集中 `CACHE_TTL_SECONDS` 與 `CACHE_BUCKET_MAX_ENTRIES`；各模組保留原有 named constants 作為相容 alias，避免改變既有呼叫端與 TTL 行為。
- TD-12：既有 per-key cache 經共用 `cache.enforce_bucket_cap()` 做 bounded eviction；snapshot cache 採整代替換；in-flight event 由 `finally` 完成清理。部署文件已補上生命週期、上限與未來外部狀態儲存的邊界。
- 未修改 baseline；`derivatives-ai.html` 與 `derivatives-analytics.html` 的既有動態畫面差異不屬本批次範圍。

驗證結果：

- Python 語法：PASS（`app.py`、`cache.py`、`security.py`、`market_config.py`、`fetchers.py`、`builders.py`、`routes_global_market.py`、`routes_twse.py`）。
- Unit：PASS，121 tests。
- Security guardrail：PASS，16 項。
- `e2e_smoke.py`：PASS（`E2E_SMOKE_OK`）；僅有目前環境無法連線 TAIFEX/Yahoo 的既有 fallback 警告。
- `git diff --check`：PASS。
- `regression/verify_against_baseline.py --full`：API cached/live、security、interaction compare 全部 PASS；frontend compare 僅剩 `derivatives-ai.html` 23.15% 與 `derivatives-analytics.html` 21.97% 動態畫面差異，未發現 API schema 或批次 D 相關回歸。

下一步：

- 進入批次 E（TD-08、TD-09、TD-14），處理前端導覽、資源與 CSS 整理。

### 2026-08-29：derivatives-ai/analytics 畫面差異處理完成

完成事件：

- 根因確認：兩頁共用的 `renderAssetHubPublicOptionChainCard()` 因批次 B 新增公開選擇權狀態提示，造成整頁高度增加約 18px；DOM 元素數量與 console error 均未退化。
- 依使用者授權，只更新 `regression/baseline/screenshots/derivatives-ai.html.png` 與 `regression/baseline/screenshots/derivatives-analytics.html.png`；未修改 API、frontend manifest 或 interaction baseline。

驗證結果：

- `regression/verify_against_baseline.py --full`：PASS（`VERIFY_OK`）；cached/live API、security、21 頁 frontend compare、94 條 interaction compare 全部通過。
- Unit：PASS，121 tests；`e2e_smoke.py`：PASS（`E2E_SMOKE_OK`）；`git diff --check`：PASS。
- 原先兩項 `[畫面差異]` 已消除，未新增 console error、API schema 或互動回歸。

下一步：

- 進入批次 E（TD-08、TD-09、TD-14），處理前端導覽、資源與 CSS 整理。

### 2026-08-29：批次 E 完成

完成事件：

- TD-08：21 個 HTML 頁面保留一致的 `.nav-links` shell，移除 20 頁會被覆寫的重複靜態 links；`renderSharedNavigation()` 移至 `js/render-shared.js` 載入完成時執行，由單一 `NAV_LINKS` 產生 18 個連結與 active state。`index.html`、`news.html` 無對應 nav 目的頁而維持無 active；`derivatives-ai.html` 依既有流程導向 `derivatives-analytics.html`。
- TD-09：確認 service worker 已啟用 stale-while-revalidate，僅快取同源靜態資源；未修改 `CACHE_VERSION`，因本批次沒有破壞既有快取契約的部署變更。
- TD-14：未改動 7 個 CSS split、`styles.css` 或 cascade 順序；新增 `regression/resource_audit.py`，固定量測 21 頁導覽一致性、宣告資源量與 service-worker 重用率。
- 資源量測：21 頁宣告的冷啟動靜態資源總量由 45,072,734 bytes 降至 45,053,393 bytes，減少 19,341 bytes；唯一宣告資源 2,145,981 bytes，跨頁可重用潛力 42,907,412 bytes（95.24%）。15 個 JS 切片共 1,715,699 bytes、`app.js` 1,456 bytes、7 個 CSS split 共 421,759 bytes。
- 快取量測：首次載入後切換至第二頁，26 個同源靜態回應中 26 個由 service worker 命中，實測命中率 100%。

驗證結果：

- Python/JavaScript syntax：PASS（批次 E 修改檔案）。
- Unit：PASS，121 tests。
- Security guardrail：PASS，16 項。
- `regression/resource_audit.py`：PASS，21 頁 runtime nav；每頁 18 links，active state 與預期一致；service-worker cache hit 100%。
- `e2e_smoke.py`：PASS（`E2E_SMOKE_OK`）；僅有目前環境對 TAIFEX/Yahoo 的既有外部 fallback 警告。
- `regression/verify_against_baseline.py --full`：PASS（`VERIFY_OK`）；cached/live API、21 頁 frontend compare、94 條 interaction compare 與 security 全部通過。
- `git diff --check`：PASS；未更新任何 screenshot、API、frontend manifest 或 interaction baseline，CSS split 未變更。

下一步：

- 批次 E 已完成；後續若要再改善載入量，需另立批次逐一評估 `legacy-unclassified.js` 或頁面級 JS 拆載，並以完整 baseline 驗證，不在本批次順手刪除。
- 維持 service worker 的 `CACHE_VERSION` 與 `pwa.js` 版本同步策略；若未來有破壞性前後端靜態資產部署，再另行遞增版本並驗證快取清理。

### 2026-08-29：批次 F 完成

完成事件：

- TD-23：移除 `app.py` module import 時的 `DERIVATIVES_STORE.initialize()`；主程式啟動明確初始化，WSGI `app:app` 則在第一個 API request lazy initialize，並保留 `DerivativesStore.initialize()` 的 thread-safe idempotent 行為。
- TD-23：新增 `import app` 不建立 SQLite 與 API request 初始化替換 store 的回歸測試。
- TD-24：測試模組以 `TemporaryDirectory` 指定 `MARKET_PULSE_CACHE_FILE`；`verify_release_integrity.py` 的子測試程序使用暫存 cache 與 `PYTHONDONTWRITEBYTECODE=1`，並將 cache hash、DB side effect 納入失敗條件。
- F-01：3 個 fixtures 已核對既有 provenance/hash：`taifex_txo_sample.html` 852 B、`twse_market_sample.json` 1183 B、`taifex_futures_oi_sample.html` 613 B。
- F-02/F-03：重建 portable ZIP，乾淨解壓後以 existing-database 與刪除 schema-only DB 的 clean-package 兩模式驗證；未新增 DB、cache 或 pycache 副作用。
- F-04：既有 `market-platform-full-history.bundle` 通過 `git bundle verify`，包含完整歷史與 tags。

驗證結果：

- Python syntax：PASS；Unit：PASS，123 tests。
- Security guardrail：PASS，16 項；`e2e_smoke.py`：PASS（`E2E_SMOKE_OK`）。
- Existing repo `verify_release_integrity.py`：PASS，`test_returncode=0`、`db_unchanged=true`、`cache_unchanged=true`、`side_effect_db_created=false`、`mock_free=true`。
- Clean package `verify_release_integrity.py`：PASS，`mode=clean_package`、123 tests、`db_unchanged=true`、`cache_unchanged=true`、`side_effect_db_created=false`、解壓目錄 `pycache_count=0`。
- Clean package `portable_check.py`：PASS（exit 0），21 HTML pages、43 URLs/assets；外部 TWSE fallback 被正確標記，不視為程式碼失敗。
- `regression/verify_against_baseline.py --full`：PASS（`VERIFY_OK`）；cached/live API、21 頁 frontend compare、94 條 interaction compare、security 全部通過。
- `git diff --check`：PASS；未修改正式 SQLite、baseline screenshot、API 或 interaction manifest。

下一步：

- 批次 F 已完成；進入批次 G（TD-07、TD-10、TD-11、TD-13，以及 TD-05/TD-01 後續品質整理）前，維持「一個小範圍、完整六層驗證、再進下一個小範圍」的節奏。
- 後續若修改 portable allowlist 或 release proof，需重新執行 existing/clean package integrity、portable smoke 與 bundle verification；不要將生成的 ZIP、bundle、SQLite 或 cache 納入版控。

### 2026-08-29：批次 G 完成

完成事件：

- TD-10：新增 `tests/fixtures/td10_large_function_contracts.json`，並補上 `build_analysis_sections`、`fetch_yahoo_margin_trading`、`build_live_sector_site_data` 的可重現 fixture 輸入/輸出特性測試；既有 `build_stock_detail`、`build_sector_fund_flow`、`build_site_data`、`build_news` 等覆蓋一併納入 126 tests 的回歸護欄。
- TD-07：確認 SSL fallback 維持「先 verified、明確 `ALLOW_UNVERIFIED_SSL_FALLBACK=1`、允許網域白名單、production 阻擋」；新增 regression assertion，確認每次 fallback 都呼叫 `DERIVATIVES_STORE.log(..., "ssl_fallback", ...)`。
- TD-11：repo-wide 檢查確認 production `app.js`/`js/*.js` 無 `worker` 同名函式；現存 `worker` 僅為 regression helper，不屬前端 runtime global symbol。
- TD-13：對 5 個原本沒有上下文的 broad `except Exception` 補上最低限度 `LOGGER.debug(..., exc_info=exc)`，保留原有 fallback 與 API 行為。
- TD-01/TD-02 後續整理：將 `renderAssetHubPage` 的 load-more、台灣選擇權 expiry/product 事件綁定收進同函式 closure；未新增 global symbol，classic-script 載入順序與輸出保持不變。

驗證結果：

- Python syntax：PASS（`builders.py`、`routes_global_market.py`、`test_derivatives_platform.py`）；JavaScript syntax：PASS（`js/page-global-market-assethub.js`）。
- Unit：PASS，126 tests；新增 G fixture tests 3 項，SSL fallback audit assertion 1 項。
- Security guardrail：PASS，16 項；`git diff --check`：PASS。
- `e2e_smoke.py`：PASS（`E2E_SMOKE_OK`）；僅記錄目前環境對 TAIFEX/Yahoo 的外部連線拒絕 fallback 警告。
- `regression/verify_against_baseline.py --full`：PASS（`VERIFY_OK`）；live/cached API、21 頁 frontend compare、94 條 interaction compare、security 全部通過。未更新 screenshot、API、frontend manifest 或 interaction baseline。

下一步：

- 批次 G 已完成；若要繼續降低前端複雜度，下一個獨立小批次處理 `renderStockDetail`、`renderTechnicalChart` 等大型 render function，逐項保留 global-symbol 與 full baseline 護欄。
- `docs/TD稽核清單.md` 的原始稽核列仍保留歷史描述；本完成事件是目前批次狀態的唯一更新來源，後續可另立文件同步稽核列的「已修復」標記。
